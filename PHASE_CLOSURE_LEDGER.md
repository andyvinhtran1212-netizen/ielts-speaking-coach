# Phase Closure Ledger

Single source-of-truth tracking page redesigns + PR refs + closure status across Phases 1-4.

**Last updated:** Sprint 10.1.5 (2026-05-15) — re-enable `needs_review` capture + dedicated Needs Review tab (5th mode card)

**Verification:** Test pin `frontend/tests/phase-closure-ledger.test.mjs` cross-references this ledger against:

- `DESIGN_SYSTEM.md` § 14.1 / § 14.2 / § 14.5 row claims
- `UNIFIED_DESIGN_BRIEF.md` § 2 phase map + cumulative count line
- Per-page file existence (deleted pages verified absent)
- `frontend/vercel.json` redirect preservation
- `admin-writing.css` `--av-text-faint` cap (≤ 10 per § 17.6)

When this ledger drifts from docs, the test fails — closure truth requires audit before proceeding. This ledger + its test pin formalize **Gate 8** (DESIGN_SYSTEM.md § 17.7).

---

## Phase 1 — Speaking flow (COMPLETE)

| Page | Path | Status | PR | Sprint |
|---|---|---|---|---|
| home | `frontend/pages/home.html` | COMPLETE | #121 | 6.3 |
| speaking | `frontend/pages/speaking.html` | COMPLETE | #123 / #124 / #125 | 6.4 / 6.4.1 / 6.4.2 |
| practice | `frontend/pages/practice.html` | COMPLETE | #127 / #128 | 6.5 / 6.5.1 |
| result | `frontend/pages/result.html` | COMPLETE | #130 / #131 | 6.6 / 6.6.1 |

**Phase 1 closure audit hotfix:** Sprint 6.6.1 (PR #131) closed cumulative Phase 1 audit (Codex AMBER #1 canonical IIFE drift + AMBER #2 architecture-doc staleness).

---

## Phase 2 — Writing + aggregate (COMPLETE)

| Page | Path | Status | PR | Sprint |
|---|---|---|---|---|
| writing-dashboard | `frontend/pages/writing-dashboard.html` | COMPLETE | #132 / #133 / #134 | 6.7 / 6.7.1 |
| writing-result | `frontend/pages/writing-result.html` | COMPLETE | #135 | 6.8 |
| full-test-result | `frontend/pages/full-test-result.html` | COMPLETE | #136 | 6.9 |

**Phase 2 closure audit hotfix:** Sprint 6.9.1 (PR #137) closed cumulative Phase 2 audit (Codex AMBER brief/test drift + central architecture-insights placement; formalized Chart.js A.2 + pre-work patterns).

---

## Phase 3 — Vocabulary + Profile + Onboarding (COMPLETE)

| Page | Path | Status | PR | Sprint |
|---|---|---|---|---|
| vocabulary | `frontend/pages/vocabulary.html` | COMPLETE | #138 | 6.10 |
| theme-toggle icon normalization (shared) | components.css + 6 redesigned pages | COMPLETE | #139 | 6.10.1 |
| my-vocabulary | `frontend/pages/my-vocabulary.html` | COMPLETE | #140 | 6.11a |
| flashcards | `frontend/pages/flashcards.html` | COMPLETE | #141 | 6.11b |
| exercises | `frontend/pages/exercises.html` | COMPLETE | #141 | 6.11b |
| profile | `frontend/pages/profile.html` | COMPLETE | #142 | 6.12a |
| onboarding | `frontend/onboarding.html` (at root) | COMPLETE | #143 | 6.12b |

**Phase 3 closure audit hotfix:** Sprint 6.12c closed cumulative Phase 3 audit + **formalized § 17 audit checklist gates** (Gates 1-7).

---

## Phase 4 — Marketing + Admin + Grammar Wiki (✅ COMPLETE)

### Marketing (2 pages)

| Page | Path | Status | PR | Sprint |
|---|---|---|---|---|
| index | `frontend/index.html` | COMPLETE | #145 / #146 | 6.13a / 6.13a-ext |
| pricing | `frontend/pricing.html` | COMPLETE | #147 | 6.13b |

**Era B reconciliation:** `frontend/landing.html` DELETED atomically Sprint 6.13a (PR #145). Was orphan with no production refs; no Vercel redirect required.

### Admin sub-pages (8 fully migrated + 1 STRUCTURALLY COMPLETE)

| Page | Path | Status | PR | Sprint |
|---|---|---|---|---|
| admin-writing | `frontend/pages/admin-writing.html` | COMPLETE | #149 | 6.14a |
| admin-writing-new | `frontend/pages/admin-writing-new.html` | COMPLETE | #149 | 6.14a |
| admin-writing-status | `frontend/pages/admin-writing-status.html` | COMPLETE | #149 | 6.14a |
| admin-writing-prompts | `frontend/pages/admin-writing-prompts.html` | COMPLETE | #149 | 6.14a (Supabase outlier) |
| admin-writing-assignments | `frontend/pages/admin-writing-assignments.html` | COMPLETE | #150 | 6.14b (Supabase outlier) |
| admin-students | `frontend/pages/admin-students.html` | COMPLETE | #150 | 6.14b |
| admin-instructor-queue | `frontend/pages/admin-instructor-queue.html` | COMPLETE | #151 | 6.14c |
| admin-writing-grade | `frontend/pages/admin-writing-grade.html` | COMPLETE | #151 | 6.14c (own grading CSS) |
| **admin** | `frontend/admin.html` (at root) | **STRUCTURALLY COMPLETE** | #153 | 6.14d-α |

**admin.html status detail:** Chrome migrated (canonical IIFE + theme toggle + tokens + `body.av-page` + foundation order). Renderer-emitted JS template inline palette **deferred to Sprint 6.14d-β / 6.14d-γ** with triggers documented `DESIGN_SYSTEM.md` § 14.5.2 / § 14.5.3. Four deferred regions are marked with `SPRINT 6.14d-α DEFERRED` comments in `frontend/admin.html` (added Sprint 6.15.3-hotfix). Treatment: **"chrome-complete, renderer-deferred"**.

**Phase 4 admin closure audit hotfix:** Sprint 6.14c-hotfix (PR #152) formalized § 17.6 shared CSS cap monitoring + § 2.1 Sprint 6.14d strategy guidance.

### Grammar Wiki cluster (5 pages)

| Page | Path | Status | PR | Sprint |
|---|---|---|---|---|
| grammar (landing) | `frontend/grammar.html` (at root) | COMPLETE | #154 | 6.15 |
| grammar-roadmap | `frontend/pages/grammar-roadmap.html` | COMPLETE | #154 | 6.15 |
| grammar-article | `frontend/pages/grammar-article.html` | COMPLETE | #154 | 6.15 |
| grammar-search | `frontend/pages/grammar-search.html` | COMPLETE | #154 | 6.15 |
| grammar-compare | `frontend/pages/grammar-compare.html` | COMPLETE | #154 | 6.15 |

**Typography sub-system:** DM Sans (body) + Lora (display) preserved per `DESIGN_SYSTEM.md` § 14.2 — decision **RESOLVED** Sprint 6.15. Editorial cluster distinct from utilitarian Plus Jakarta Sans pages.

**Phase 4 closure events:**
- Sprint 6.15.2 (PR #155) — narrative correction (dashboard.html non-existence + zero `--ds-*` state)
- Sprint 6.15.3-hotfix (PR #156) — closure audit AMBER closures + Gate 8 formalization
- Sprint 6.15.4-hotfix (PR #157) — **RED bug fix**: light-theme `text-white/XX` opacity-variant coverage gap (cascade-winning override blind spot, same class as Sprint 6.10.1)
- Sprint 6.16 (PR #158) — **fix sprint** (not audit hotfix): cross-page navigation label drift ("Dashboard" → "Trang chủ" / "Quay lại") + Speaking page IA cleanup
- Sprint 6.15.5-hotfix (PR #159) — **2nd RED bug fix on grammar-article.html**: Sprint 6.15.4 covered class-bearing elements but markdown emits class-less `<p>` / `<h2>` / `<li>` / `<td>` etc. that inherited Tailwind's raw `text-white` from the body element (descendant override couldn't match body itself). Fix: drop `text-white` from grammar-article body + defensive token-driven overrides in grammar-wiki.css.
- Sprint 6.15.6-hotfix (PR #160) — **3rd RED bug fix, comprehensive component pass**: cards (Học tiếp theo / Bài liên quan / category cards) invisible on all 5 grammar pages in light theme despite Sprint 6.15.4 covering every emitted `text-white/X` variant. Phase A audit surfaced multi-mechanism root cause. Fix bundles **6 items**: (1) `text-white` dropped from body on remaining 4 grammar pages (mirror Sprint 6.15.5); (2) 3 JS inline-color sites in grammar.js refactored to class hooks (`.gw-status-dot--planned`, `.gw-status-badge--planned`, `.gw-save-btn`, `.gw-progress-track`); (3) `.cat-card` / `.article-card` / `.group-card` / `.group-article-row` component class hooks gain explicit `--av-text-*` color rules; (4) card surfaces switch from invisible `bg-white/[0.03]` to `--av-surface-card`; (5) `body.av-page.text-white` compound-selector defensive guard; (6) Sprint 6.15.6-hotfix marker + 31-pin regression test file. **Strategy change vs prior 3 hotfixes:** comprehensive Phase A audit before fix (4th iteration warranted broader scope than L3 single-root-cause discipline).
- Sprint 6.15.7-hotfix (PR #161) — **theme toggle structural placement fix on 4 grammar sub-pages**: Andy reported toggle visible top-left + both icons stacked + no click response on grammar-article.html. Phase A bisect of 5 stacked PRs (#156-160) confirmed **none touched components.css, theme-toggle.js, or toggle markup**. True culprit traced to **Sprint 6.15 (PR #154)** — the malformed nav layout has existed since the original Grammar Wiki ship. Surgical 3-item fix: (1) restructure 4 grammar sub-page navs so the "Trang chủ" link + theme toggle share a single `<div class="flex items-center gap-2|3">` chrome wrapper (grammar-article moves the button INSIDE the existing right-side wrapper; roadmap/search/compare wrap their previously-standalone link+button in a new flex div); (2) new `theme-toggle-layout-context.test.mjs` (68 pins) — structural sentinel that walks tag-depth and asserts the toggle's immediate parent is a flex container; coverage extends to all 29 pages carrying the toggle (catches the same drift class on any future page); (3) switch all 5 grammar pages to absolute `/js/theme-toggle.js` import path so Vercel `/grammar/:category/:slug` rewrites don't break relative module resolution. **Audit gap closed:** Sprint 6.10.1 Gate 3 checked markup presence but not DOM structural correctness. Filing § 17.8 Gate 9.7 (per-component verification + structural-context sentinel) as the formal lesson.
- Sprint 6.15.8-hotfix (PR #162) — **CSS path absolutization, Vercel-rewrite 404 fix**: Andy's DevTools surfaced `tokens.css` + `components.css` returning **404** when grammar-article.html is served under the `/grammar/:category/:slug` rewrite. Root cause: every redesigned page used relative stylesheet paths (`../css/...` for `/pages/*` files, `css/...` for root files), and the browser resolves them against the served URL — not the rewritten target — so `/grammar/foo/bar` makes `../css/tokens.css` 404 at `/grammar/css/tokens.css`. Same class of bug Sprint 6.15.7-hotfix Item 3 fixed for `theme-toggle.js`; now generalized to CSS. Same rewrite class also affects `/writing/dashboard`, `/admin/writing/prompts`, `/admin/writing/assignments`, `/home`, `/speaking` — so the fix is repo-wide, not grammar-only. **Atomic 3-item fix:** (1) convert 106 `<link rel="stylesheet">` hrefs to absolute paths across 29 redesigned pages (24 subfolder files: `../css/X` → `/css/X`; 5 root files: `css/X` → `/css/X`); (2) new `css-paths-absolute.test.mjs` (35 pins) — sentinel asserts every stylesheet href on redesigned pages starts with `/` or is a full URL; dedicated pins on all 5 grammar pages for the canonical 4-import set; (3) ledger + ledger-test count bump. **Audit gap closed:** the `/js/theme-toggle.js` fix in 6.15.7-hotfix Item 3 was per-asset; this hotfix generalizes the principle (all rewrite-served pages need absolute relative-resource paths). Gate 9.7 lesson reinforced: per-asset fixes must trigger systematic same-class audits.
- Sprint 6.16.1 (PR #163) — **audit methodology formalization, Gate 9.5 + 9.6 + 9.7 triple bundle**: pure docs sprint. The 5 cumulative audit blind spots (Sprint 6.10.1 + 6.15.4 + 6.15.5 + 6.15.6 + 6.15.7) had been recognized inline in their respective hotfix bodies but not consolidated into formal § 17 audit gates. Sprint 6.16.1 formalizes the three lessons each filed against a hotfix that surfaced them: **§ 17.9 Gate 9.5** (Runtime-render inheritance — filed Sprint 6.15.5: body utilities inherit into class-less markdown / template-literal output; descendant overrides can't match the body element itself), **§ 17.10 Gate 9.6** (Structural layout context — filed Sprint 6.15.7: sentinel tests that verify markup existence must also verify DOM-tree parent-child layout context, especially for chrome controls), **§ 17.11 Gate 9.7** (Per-component theme verification — filed Sprint 6.15.6: page-level "readable" smoke missed 5 mechanisms across multi-component pages; per-component DevTools inspection × both themes is mandatory). Plus **§ 17.12** records the 5-instance blind-spot evolution pattern + 6 pre-empt questions for future audit reviewers, and **§ 17.13** consolidates the cumulative 12-gate table. New `gate-9-5-9-6-9-7-formalization.test.mjs` pins each new section's presence + Sprint origin. **No production code changes.** Audit gate count 9 → 12; audit hotfix count unchanged at 13 (Sprint 6.16.1 is methodology, not hotfix).
- Sprint 6.17 Phase C1 (PR #164) — **chrome unification foundation + 3-page canonical migration**: Andy reported 4+ chrome variants across redesigned pages (full nav / minimal / breadcrumb / skill-module). Phase A audit (29 pages) surfaced 5 chrome categories. Andy approved Phase B scope: Cat 1+2 only (11 pages migrate, Cat 3 grammar / 4 marketing / 5 admin explicitly excluded as legitimate separate IA tiers). Phase C scope subdivided into C1 (foundation + 3 pages) shipped this PR and C2 (8 remaining pages, Sprint 6.17.1) deferred for per-page JS-contract preservation work. **C1 ships:** (1) canonical chrome rules moved into `components.css` so all Cat 2 adopters pick up the foundation from a single source (`.shell` + `.topnav-wrap` lightweight wrapper + `.topnav` + `.brand` + `.nav-links` + `.topnav-right` + `.user-pill` + `.user-menu` dropdown family + mobile breakpoint @media 720px); (2) NEW `/js/user-pill.js` shared ES module — idempotent `bindUserPill()` wires the toggle + outside-click + Escape + Đăng xuất via `window.getSupabase().auth.signOut()`; (3) `home.html` + `vocabulary.html` canonical references updated to dropdown user-pill (vocabulary gains avatar; previously absent); (4) `profile.html` migrated to canonical full nav (Trang chủ active; previous `pf-header` chrome retired); (5) NEW `chrome-unification-canonical.test.mjs` (47 pins) — pins foundation rules in components.css + user-pill.js module contract + canonical chrome on 3 migrated pages + deferred roster tracking. **Decisions captured:** logo wordmark canonicalized to `Aver<span class="dot">.</span>Learning` form across Cat 2; `Đăng xuất` integrated into user-pill dropdown (single canonical pill behavior across all pages); active-tab mapping documented per page (Trang chủ / Writing / Speaking / Vocabulary). **Atomic spirit preserved:** foundation is the shippable closed concern; bulk migration is mechanical follow-up per Sprint 6.10.1 precedent. Audit hotfix count unchanged at 13 (Sprint 6.17 is a fix sprint, not audit hotfix).
- Sprint 6.17.1 (PR #165) — **chrome unification Phase C2: 10 remaining Cat 2 pages migrated to canonical full nav**. Completes the chrome-unification sprint started in PR #164 (Phase C1). Phase A per-page JS-contract audit produced migration plans; Andy Phase B approved 4 decisions: (Q1) extend `embedded-mode.css` selector to include canonical `.topnav-wrap`; (Q2) canonicalize all logout flows on `/index.html` redirect; (Q3) drop emoji from onboarding chrome (canonical brand only); (Q4) preserve practice session-context info via new `<div class="practice-context-bar">` below canonical nav. **Pages migrated in this PR (10 in low → high risk order):** `result.html` + `writing-result.html` + `practice.html` + `full-test-result.html` (Speaking/Writing active; theme-toggle migrated to `/js/theme-toggle.js` module; Lucide re-render via MutationObserver on `data-theme` for chevron stroke updates + full-test-result Chart.js radar refresh); `onboarding.html` (Trang chủ active; 🎙️ emoji dropped from wordmark per Q3); `my-vocabulary.html` + `flashcards.html` + `exercises.html` (Vocabulary active; embedded-mode contract preserved — `embedded-mode.css` extended with `html.embedded-mode > body > .topnav-wrap` selector to hide canonical chrome when iframe-mounted via `?embedded=1`); `writing-dashboard.html` (Writing active; legacy `#logout-btn` removed, logout integrated into canonical user-pill dropdown); `speaking.html` (Speaking active; entire custom `#avatar-menu` dropdown replaced by canonical pill — 8 legacy IDs retired: `#avatar-wrap`/`#avatar-menu`/`#avatar-img`/`#avatar-initials`/`#btn-logout`/`#user-name`/`#user-name-skeleton`/`#avatar-dropdown-wrap`; inline JS rewired to populate `#user-pill-name` + `#user-avatar`; outside-click and logout handlers deleted as `/js/user-pill.js` owns both). **Tests:** `chrome-unification-canonical.test.mjs` extended from 3 to 13 canonical pages + new embedded-mode contract sentinel suite (3 pages × IIFE preservation pin + `embedded-mode.css` selector pin); 10 per-page redesign test files refreshed (toggle pin pattern updated from `querySelector('.av-theme-toggle')` to canonical module import; legacy chrome IDs replaced by canonical pill IDs; back-link/emoji pins replaced by canonical-chrome pins where applicable); `cross-page-navigation-canonical.test.mjs` href pins updated to absolute `/pages/speaking.html` form on result/practice/full-test-result. **Audit hotfix count unchanged at 13** (Sprint 6.17.1 is a fix sprint completing Sprint 6.17 — not audit hotfix).
- Sprint 10.1.5 (this PR) — **Capture re-architecture: re-enable `needs_review` + dedicated Needs Review tab (5th mode card).** Andy's redesign reverses the Sprint 6.0 archival of `source_type='needs_review'` from the capture pipeline. Sprint 6.0's reasoning was that surfacing `needs_review` items in the main vocab bank encouraged learners to memorise wrong forms; Sprint 10.1.5's reasoning is that the items ARE useful as a "learning from mistakes" surface — just not in the same bucket as items the learner used correctly. The solution: re-enable persistence, default-exclude from the main bank list endpoint, surface on a dedicated Needs Review tab in vocabulary.html as a 4th vocab-module sibling alongside my-vocab / flashcards / exercises. **Phase B Andy decisions (4, all "go with recommendations"):** (Q1) Soft delete (`is_archived=true`) preserves restorability; (Q2) NEW 5th mode card in vocabulary.html with `alert-circle` Lucide icon; (Q3) Re-enable `needs_review` capture into the dedicated tab (NOT the main bank); (Q4) Card visual = `original_word → suggestion` arrow + context + AI feedback, Mark-fixed promotes to `source_type='manual'`, AI feedback verbatim, newest-first ordering; (Q5) Hide "Needs review" filter pill from My Vocab Bank (legacy items move to new tab automatically via the backend filter switch). **Backend changes:** (a) `routers/grading.py:_PERSISTED_SOURCE_TYPES` — `needs_review` re-added to the frozenset alongside `used_well` + `upgrade_suggested` (Sprint 6.0 archival reversed; multi-line comment block updated to document the reasoning AND the new routing — main bank vs Needs Review tab); (b) `routers/vocabulary_bank.py` `GET /` list endpoint — default-excludes `source_type='needs_review'` via a `.neq("source_type", "needs_review")` filter when the caller does NOT pass an explicit `?source_type=` query param (admins / tests can still inspect needs_review rows via `?source_type=needs_review` — the filter is honoured if explicitly set); (c) NEW `GET /api/vocabulary/bank/needs-review` endpoint — returns alive `source_type='needs_review'` items ordered by `created_at DESC`, scoped to the user via Supabase RLS, fires a `vocab_needs_review_viewed` analytics event for observability; (d) NEW `POST /api/vocabulary/bank/{id}/restore` endpoint — flips `is_archived=true` back to `false` (idempotent: restoring an already-alive row returns `{ok:true, already_alive:true}` without DB write). The existing `DELETE /api/vocabulary/bank/{id}` continues to perform soft delete (`is_archived=true`) per the Sprint 7.x contract — no semantic change needed. The existing `POST /api/vocabulary/bank/{id}/mark-fixed` endpoint is reused as-is: it already flips `source_type='needs_review' → 'manual'` (Sprint 6.0 era kept the endpoint alive for triage of legacy items even after archival; Sprint 10.1.5 just routes new-capture items through the same flow). **Frontend changes:** (a) NEW `frontend/js/vocab-modules/needs-review.js` — 4th vocab-module sibling. Same `mount(container, opts) → { unmount }` contract as my-vocab / flashcards / exercises. Uses `.subpage-header` with `.subpage-header__back` Vocabulary back-link (Sprint 9.2 pattern). Card layout: `<span class="nr-card__original">{original_word}</span>` (strikethrough faded) → arrow-right Lucide icon → `<span class="nr-card__suggestion">{suggestion}</span>` (primary color, semibold) + `<p class="nr-card__context">"{context_sentence}"</p>` (italic, verbatim) + `<p class="nr-card__feedback">{reason}</p>` (AI's reason field verbatim) + 2 action buttons (`✓ Đã sửa, đưa lên flashcard` → `mark-fixed`, `🗑 Bỏ qua` → soft-delete). Intro banner (`.needs-review-intro`) frames the surface pedagogically so it reads as "AI-flagged learning opportunities" rather than "things you got wrong". Toast feedback on action success/failure. Embedded-vs-standalone routing via `opts.embedded`. Lucide `arrow-right` re-hydrated per card after `innerHTML` swap (Sprint 9.3 pattern). HTML template body contains 0 backticks (Sprint 9.1.1-hotfix sentinel preserved). (b) NEW `frontend/css/needs-review.css` — 130-line page-local stylesheet scoped to `.nr-*` BEM family. `.nr-card` shares `--av-surface-card` / `--av-border-subtle` / `--av-radius-lg` with the canonical primitives but is its OWN component (not a `.mode-card`) — different layout (flex-column with header-row inside) and different click semantic (action buttons inline, not click-to-navigate). Distinct hover treatment (border darken, no translateY). `.nr-card__btn--fix` uses `--av-primary` family; `.nr-card__btn--dismiss` uses neutral border. `.nr-toast` mirrors flashcards.css toast for visual consistency. (c) `frontend/pages/vocabulary.html` — 5th `.mode-card` added to the dashboard grid (`data-mode="needs-review"` + Lucide `alert-circle` icon + "Từ vựng và cách diễn đạt cần xem lại." lede) sitting between `exercises` and `topic-bank`. NEW `<section class="tab-panel" data-panel="needs-review" id="panel-needs-review">` with `<div class="tab-mount" id="mount-needs-review">` inside. NEW `<link rel="stylesheet" href="/css/needs-review.css">` alongside the existing my-vocabulary/flashcards/exercises stylesheets. (d) `frontend/js/vocab-landing.js` — `TAB_LOADERS` extended with `'needs-review': () => import('/js/vocab-modules/needs-review.js')`; `VALID_TABS` Set extended from 4 to 5 entries; `DEFAULT_TAB` stays `'my-vocab'` (unknown-mode fallback). Hash-routing now supports `vocabulary.html#needs-review` deep-links. (e) `frontend/js/vocab-modules/my-vocab.js` — `<button class="filter-btn" data-action="set-filter" data-filter="needs_review">` filter pill retired (replaced by retirement comment explaining the Sprint 10.1.5 routing). The pre-10.1.5 `_applyFilter()` switch still accepts `'needs_review'` as a value but the backend list endpoint's default-exclude means the filter would return empty even if pressed externally — no JS-side cleanup needed. **Test changes (4 files):** **(a) `vocab-module-loader.test.mjs`** — 1 stale pin flipped: "HTML template ships all 7 filter buttons" → "the 6 active filter buttons (Sprint 10.1.5 retired needs_review pill)" + negative pin guarding against regression. NEW 3 Sprint 10.1.5 describe blocks (14 pins): (i) needs-review.js module contract — 9 pins covering exports, guardMount + opts.embedded, canonical .subpage-header with back-link, card layout (`.nr-card__original` / `.nr-card__suggestion` / `.nr-card__context` / `.nr-card__feedback` / mark-fixed + dismiss data-actions), 3 required data-actions in switch, backend `/needs-review` endpoint call, mark-fixed → `/mark-fixed` promote endpoint, dismiss → DELETE method, **Sprint 9.1.1-hotfix backtick-in-template-literal sentinel**; (ii) vocabulary.html surfaces — 3 pins (5th mode card with alert-circle icon + needs-review label, panel-needs-review tab-panel hidden by default + mount-needs-review container, needs-review.css stylesheet link); (iii) vocab-landing.js wiring — 2 pins (TAB_LOADERS registers needs-review dynamic-import, VALID_TABS Set lists needs-review alongside the other 4). **(b) `vocab-landing.test.js`** — `'all 3 vocab children are registered in TAB_LOADERS'` → 4 vocab children (`'exercises', 'flashcards', 'my-vocab', 'needs-review'`); `'VALID_TABS surface lists exactly the four supported modes'` → five supported modes (`'exercises', 'flashcards', 'my-vocab', 'needs-review', 'topic-bank'`). **(c) `vocabulary-redesign.test.mjs`** — `requiredPanelIds` + `requiredDataPanels` rosters extended to 5 entries including `panel-needs-review` / `needs-review`; the iterated `data-panel`, mode-card data-mode, and panel-hidden tests scale automatically with the roster (1 new test per check × the new entry = ~3 net new pins). `role="tabpanel" preserved on 4 panels` → 5 panels; `all 4 panels start hidden` → all 5 panels start hidden. **(d) No backend test additions** (the new `/needs-review` and `/restore` endpoints reuse the existing auth + feature-flag pattern from neighbouring vocab_bank endpoints; backend test infrastructure for those uses a different fixture stack that's out of scope for this front-end-heavy sprint — backend pytest additions deferred to a follow-up if Andy wants explicit endpoint-level coverage). **Frontend gate: 2634/2634 pass** (net +17 vs Sprint 9.3 baseline 2617 — 14 new Sprint 10.1.5 pins + 3 pin-flip extensions from `requiredDataPanels` roster expansion). **Audit hotfix count unchanged at 18** (Sprint 10.1.5 is a capture-re-architecture feature, not audit hotfix or methodology). **Pre-push verification:** `grep -rn 'data-mode="needs-review"' frontend/` → 1 production match (vocabulary.html dashboard mode card); `grep -c 'data-action="back-to-dashboard"' frontend/js/vocab-modules/needs-review.js` → 1 (Sprint 9.2 pattern adopted); `grep -n "_PERSISTED_SOURCE_TYPES" backend/routers/grading.py` → 3 matches (definition + 2 references, all in `grading.py`). **NO scope creep:** SRS state untouched (Sprint 10.2 candidate per Sprint 10.0 discovery roadmap Option A Sprint 10.2 / Option C item #2); D1 exercise feedback loop untouched (Sprint 10.3 candidate); capture confirmation UI on `result.html` NOT added (Sprint 10.4 candidate); AI extraction prompt unchanged (Claude Haiku already returns `needs_review` items — Sprint 6.0 only stopped persistence, not extraction); lemmatization NOT added (separate Sprint 10.x track per discovery); chrome component untouched; backend test infrastructure additions deferred. **Backward compatibility:** legacy `source_type='needs_review'` items (captured pre-Sprint-6.0 in 2025 or via `mark-fixed` triage workflow) automatically surface in the new Needs Review tab without a migration step — the new `GET /needs-review` endpoint filters on `source_type='needs_review' AND is_archived=false AND is_skipped=false` which matches all legacy alive rows. **Architectural milestone:** Sprint 10.1.5 is the first Sprint 10.x cluster implementation following the Sprint 10.0 discovery audit. It addresses Andy's complaint #1 (capture filter) via the routing redesign — needs_review items still get captured but go to a separate surface where the learner can choose to fix-then-promote (`mark-fixed`) or dismiss (soft-delete), rather than polluting the main bank silently. The pattern is reusable: future AI verdict categories (e.g. "register mismatch", "discourse marker overuse") could ship as additional tabs with the same `.subpage-header` + back-link + card-action skeleton. The 4th lazy-loaded vocab-module also stress-tests the Sprint 7.6 module-loader contract — `needs-review.js` follows the same `mount()/unmount()` lifecycle as the other 3 modules and the loader code in vocab-landing.js needed no changes other than the TAB_LOADERS + VALID_TABS entries.
- Sprint 10.0 (PR #193) — **Vocabulary workflow discovery audit (no production code).** See preceding ledger entry for full content (PR #193 still open at time of Sprint 10.1.5 ship; this row is the canonical post-merge reference).
- Sprint 9.3 (PR #192) — **Flashcards stacks visual unification with canonical `.mode-card`.** Andy reported the 3 flashcards stack cards ("Tất cả từ vựng" / "Mới thêm gần đây" / "Cần ôn tập") were visually inconsistent with the rest of the vocabulary cluster — they shipped 3D emoji icons (📚 / 🆕 / 🎯 / 📂 for manual), an ad-hoc top-right pill ("Tự động" / "Thủ công"), and `.stack-icon` / `.stack-name` / `.stack-meta` inner classes while every other `.mode-card` adopter (3 vocab dashboards + 3 speaking dashboards + home + exercises drills) used the canonical `.head > .icon + .arrow, h3, .lede` skeleton with Lucide outline icons. Sprint 9.3 promotes the stack cards to the canonical primitive and introduces a single new `.mode-card__badge` inline element to preserve the auto-vs-manual semantic distinction. **Phase B Andy decisions (5):** (Q1) IA preserved — 3 default stacks + "+ Tạo stack mới" button + "X thẻ" count; (Q2) Lucide outline icons replacing 3D emoji — `library` for "Tất cả từ vựng", `sparkles` for "Mới thêm gần đây", `target` for "Cần ôn tập", `folder` for manual stacks; (Q3) "Tự động" indicator moved from top-right corner pill to inline badge inside the h3 title row (preserves the auto-vs-manual semantic while returning the corner slot to the canonical `→` arrow); (Q4) "X thẻ" count promoted to `<p class="lede">` (canonical description slot); (Q5) Active state dropped — clicking a stack navigates to the review session, no "selected" affordance needed (matches the other 10 `.mode-card` surfaces' click→navigate behavior). **JS — flashcards.js (markup refactor):** (a) `autoCard(s)` — emoji-to-Lucide icon mapping (`auto:all_vocab` → `library`; `auto:recent` → `sparkles`; fallback → `target`); ad-hoc top-row `<div class="flex items-start justify-between"><span class="stack-icon">{emoji}</span><span class="pill-auto">Tự động</span></div>` replaced with canonical `<div class="head"><div class="icon"><i data-lucide="{iconName}"></i></div><span class="arrow" aria-hidden="true">→</span></div>`; `<p class="stack-name">` → `<h3>{name}<span class="mode-card__badge">Tự động</span></h3>`; `<p class="stack-meta">` → `<p class="lede">{count} thẻ</p>`; (b) `manualCard(s)` — same canonical skeleton with Lucide `folder` icon, NO `.mode-card__badge` (the absence of the "Tự động" tag is itself the manual-vs-auto signal — manual stacks already carry the hover-revealed `.delete-btn` overlay as the secondary affordance); `data-action="open-stack"` + `data-stack-id` + `data-card-count` attributes preserved byte-identical so the existing event delegation continues to route clicks to the stack-review page (`/pages/flashcard-study.html?stack=...`); (c) `setFcContainerHtml(html)` — extended to re-hydrate Lucide icons after every `innerHTML` swap (`window.lucide.createIcons()` call) since the dynamically-rendered `<i data-lucide="...">` placeholders need expansion to inline SVGs. The pre-9.3 emoji icons rendered as plain text and didn't need this; canonical Lucide outlines do. **CSS — components.css (NEW primitive member):** `.mode-card__badge` rule added under the existing `.mode-card` family. Visual treatment uses `--av-info` family (info-soft background + info-strong border + info text + `--av-radius-pill`) to visually echo the pre-9.3 `.pill-auto` styling so the auto-vs-manual signal stays consistent across the redesign. Also adds `.mode-card h3 { flex-wrap: wrap; }` so the title + badge can wrap onto a 2nd line on narrow viewports (Sprint 9.1's base `.mode-card h3` already had `display: flex` + `gap`, this is the only addition). Markup-contract block comment above `.mode-card` updated to document both `.lock-tag` (Sprint 9.1 muted "Soon" affordance) AND `.mode-card__badge` (Sprint 9.3 active state) as the two recognised h3-inline-badge variants. **CSS — flashcards.css (legacy retirement):** Sprint 9.1 had left 5 stack-specific inner-class rules behind (`.stack-icon` 28px emoji size + `.stack-name` 15px typography + `.stack-meta` 12px muted typography + `.pill-auto` info-family pill + `.pill-manual` primary-family pill) — Sprint 9.3 retires all 5 (replaced by retirement comment pointing at the canonical skeleton). Only `.delete-btn` survives (manual-stack-specific hover-revealed overlay — no other `.mode-card` surface needs a removal control, so this stays page-local). Net -28 LOC. **Test changes (1 file, all NEW pins — no stale pin flips):** **`vocab-module-loader.test.mjs`** — NEW Sprint 9.3 describe blocks (11 pins): (a) "flashcards stack cards render canonical .mode-card" — 5 tests: autoCard() canonical skeleton (`.head` / `.icon` / `.arrow` / `<h3>` / `.mode-card__badge` / `.lede`), autoCard() Lucide icon mapping (library/sparkles/target + emoji absence guard), manualCard() canonical skeleton (`folder` icon + no badge + no 📂), legacy `.stack-icon`/`.stack-name`/`.stack-meta`/`.pill-auto`/`.pill-manual` retirement guard, setFcContainerHtml Lucide hydration; (b) ".mode-card__badge primitive lives in components.css" — 4 tests: badge rule with `--av-info` family styling, `.mode-card h3` keeps Sprint 9.1 `display:flex` + gains Sprint 9.3 `flex-wrap:wrap`, flashcards.css no longer declares the 5 legacy classes, flashcards.css keeps `.delete-btn` (manual-stack-specific). **Frontend gate: 2617/2617 pass** (net +9 vs Sprint 9.2 baseline 2608 — 11 new Sprint 9.3 pins added). **Audit hotfix count unchanged at 18** (Sprint 9.3 is a visual-consistency feature, not audit hotfix or methodology). **Pre-push verification:** `grep -rn "stack-card\|stack-icon\|stack-name\|stack-meta\|pill-auto\|pill-manual" frontend/css/ frontend/js/` → 0 live class references; all remaining matches are retirement comments or text references (e.g. `stack-card click bubbles` comment context, `data-stack-id` attribute on my-vocab.js). Sprint 9.1.1-hotfix backtick-in-template-literal sentinel still green — programmatic count: 0 backticks inside flashcards.js HTML template body. **NO scope creep:** Stack data model untouched; "+ Tạo stack mới" button + modal flow untouched; click navigation handler untouched (`data-action="open-stack"` + `data-stack-id` attributes byte-identical so existing `openStack(btn)` continues to route correctly); backend untouched; stack-review page (`/pages/flashcard-study.html`) untouched; manual-stack delete flow untouched (`data-action="delete-stack"` + `.delete-btn` overlay preserved). **Architectural milestone:** Sprint 9.3 completes the Sprint 9.x visual-consistency arc through the vocabulary cluster. Cumulative `.mode-card` surface count: 13 (3 vocab dashboard cards + 4 home skill cards + 3 speaking dashboard cards + 2 exercises drill cards + 3+N flashcards stacks). The new `.mode-card__badge` primitive is reusable on any future `.mode-card` adopter that needs an inline status indicator inside h3 (e.g., "Mới" / "Premium" / "Beta" / status indicators for future feature flags). The Sprint 9.x arc — 9.0 discovery, 9.1 hard refactor + primitive lift, 9.1.1-hotfix template-literal hygiene, 9.2 in-page back-link + eyebrow promotion, 9.3 stacks unification — closes with the entire vocabulary cluster visually consistent with the broader Aver Design System. Next consideration after Sprint 9.3 is writing-dashboard IA decision (Andy SKIPPED earlier) or Phase 5+ feature work.
- Sprint 9.2 (PR #191) — **In-page back-link in `.subpage-header` for vocab + speaking mode panels.** Andy reported that after entering a vocab sub-tab (my-vocab / flashcards / exercises) or a speaking mode (Luyện tập / Luyện từng Part / Full Test), the only way to return to the parent dashboard was to click the chrome's "Vocabulary" / "Speaking" nav link → full page reload. Slow + breaks browser back history. Sprint 9.2 promotes the static `<p class="eyebrow">{Skill}</p>` tier label inside each mode panel's `.subpage-header` (Sprint 9.1 primitive) to an interactive `<button class="subpage-header__back" data-action="back-to-dashboard">` so users can return to the parent dashboard view without reloading the page. Revives the design intent of the dead `.{prefix}-back-link` CSS rules that Sprint 9.0 discovery audit flagged. **Phase B Andy decisions (4):** (Q1) Button label "Vocabulary" / "Speaking" — contextual breadcrumb, no translation overhead, matches the pre-9.2 eyebrow text; (Q2) `arrow-left` Lucide icon — proven affordance, matches Sprint 8.x mode-card arrows; (Q3) speaking sub-pages adopt `.subpage-header` for the first time (Sprint 9.1's Phase D Q7 had deferred speaking from the vocab refactor — Sprint 9.2 brings them in for the back-link AND the canonical header primitive); (Q4) browser back button works for vocab via `history.pushState` (replaces the pre-9.2 `replaceState` so each mode visit becomes a history entry). **CSS — components.css (NEW primitive member):** `.subpage-header__back` rule added under the existing `.subpage-header` family. Visual treatment matches the pre-9.2 `.eyebrow` it replaces (uppercase, `--av-fw-semibold`, `--av-fs-xs`, `--av-tracking-wide`, `--av-primary` color, `var(--av-duration-fast)` transitions) plus a `translateX(-2px)` on hover for affordance and `:focus-visible` ring via `--av-shadow-focus`. Inline `svg` sized to 14px to fit the line height. Markup-contract block comment above `.subpage-header` updated to document the Sprint 9.2 promotion. **JS — 3 vocab modules (my-vocab.js / flashcards.js / exercises.js):** (a) HTML template: `<p class="eyebrow" style="margin:0;">Vocabulary</p>` replaced with `<button type="button" class="subpage-header__back" data-action="back-to-dashboard" aria-label="Quay về dashboard Vocabulary"><i data-lucide="arrow-left"></i><span>Vocabulary</span></button>`; (b) NEW `backToDashboard()` helper inside `mount()` closure — checks `opts.embedded`: embedded mode (inside `vocabulary.html`) clears the hash via `history.pushState(null, '', window.location.pathname)` + `window.dispatchEvent(new HashChangeEvent('hashchange'))` so vocab-landing's hashchange listener restores the dashboard; standalone mode (`/pages/my-vocabulary.html` etc.) hard-navigates to `/pages/vocabulary.html`; (c) handler wiring — my-vocab + flashcards add `case 'back-to-dashboard': return backToDashboard();` to their existing `switch(action)` in `handleClick()`; exercises (which had no interactive handlers per Sprint 7.5's Phase B contract) adds a single new `container.addEventListener('click', ...)` for the back-link only. **JS — vocab-landing.js (parent orchestrator):** (a) `activateTab()` swaps `history.replaceState` → `history.pushState` so mode-card clicks become history entries (Phase B Q4 — browser back support); (b) NEW `showDashboard()` exported on `window.__vocabLanding`: reveals `.vocab-modes` (sets `hidden=false`) + hides every `.tab-panel`. Symmetric to the activateTab() panel-toggle branch; (c) hashchange listener extended — if `fromHash` is in `VALID_TABS`, call `activateTab(fromHash, {updateHash:false})` as before; otherwise (empty or unknown hash), call `showDashboard()`. Both the in-page back-link and the browser Back button funnel through this same listener. **HTML — speaking.html (3 mode panels):** `#tab-practice` + `#tab-partbpart` + `#tab-fulltest` panels — pre-9.2 ad-hoc header (`<div class="mb-8"><p class="eyebrow">Speaking</p><h1 class="text-2xl font-bold mb-1">{title}</h1><p class="text-sm" style="...">{description}</p></div>`) replaced with the canonical `.subpage-header` primitive (`<header class="subpage-header mb-2"><div class="subpage-header__lhs"><button class="subpage-header__back" data-action="back-to-dashboard" aria-label="Quay về dashboard Speaking">…</button><span class="subpage-header__sep">|</span><h1 class="subpage-header__title">{title}</h1></div></header>`). The description paragraph stays as a separate `<p class="text-sm mb-8">` below the header (the `.subpage-header` primitive doesn't carry a description slot — keeping it external preserves the visual rhythm without changing the primitive). **JS — speaking.html inline (back-link delegation):** Sprint 8.1's `DOMContentLoaded` handler (mode-card click delegation) extended with a `document.addEventListener('click', ...)` for `[data-action="back-to-dashboard"]` scoped to `#tab-practice, #tab-partbpart, #tab-fulltest` selectors — calls `switchMainTab('dashboard')`. Speaking uses internal tabs (no URL hash routing) so this is a pure JS state change; browser back/forward doesn't navigate between speaking modes (Phase B Q4 — scope decision: only vocab gets history-based back). **Test changes (5 files):** **(a) `vocab-module-loader.test.mjs`** — 3 stale `HTML template ships the canonical subpage-header + Vocabulary eyebrow` pins retargeted from `class="eyebrow"` to `class="subpage-header__back"` + `data-action="back-to-dashboard"` + `<span>Vocabulary</span>` (one per module); 1 stale `exercises template has no interactive handlers` pin updated to allow exactly the `back-to-dashboard` data-action (drill cards still plain links); NEW Sprint 9.2 describe block (10 pins): 3 modules × 2 pins (markup contract + JS handler reference) + 4 vocab-landing.js pins (showDashboard() existence + body, pushState not replaceState, hashchange fallback to showDashboard, window.__vocabLanding test seam exports showDashboard). **(b) `subheading-pattern-canonical.test.mjs`** — Sprint 7.3 + Sprint 7.4/7.5 `module template carries <p class="eyebrow">Vocabulary</p>` describe blocks (3 stale pins total) replaced with a single Sprint 9.2 describe block: 3 modules × 2 pins (positive: back-link button shipped; negative: legacy `<p class="eyebrow">Vocabulary</p>` is gone). Net 6 pins replacing 3. **(c) `speaking-redesign.test.mjs`** — NEW Sprint 9.2 describe block (7 pins): 3 panels × 2 pins (markup contract including aria-label + arrow-left icon + Speaking label + title; negative: pre-9.2 `<p class="eyebrow">Speaking</p>` is gone) + 1 inline-JS delegation pin (data-action selector + switchMainTab('dashboard') call + 3-panel scoping). **Frontend gate: 2608/2608 pass** (net +17 vs Sprint 9.1.1-hotfix baseline 2588 — Sprint 9.2 added 23 new pins across vocab + speaking, retired 6 stale pins via pin-flip-and-extend). **Audit hotfix count unchanged at 18** (Sprint 9.2 is a feature add, not audit hotfix or methodology). **Pre-push verification:** `grep -rn "data-action=\"back-to-dashboard\"" frontend/` → 6 matches (3 vocab modules + 3 speaking mode panels) as expected. **NO scope creep:** Topic Bank card behavior unchanged (still placeholder); mode panel content untouched; backend untouched; chrome component untouched; deep-linking via hash still works (vocab-landing's bootstrap branch reading `window.location.hash` is unchanged for non-empty `VALID_TABS` hashes); the writing-dashboard mode entry pattern is NOT touched (Sprint 9.3 candidate if Andy decides to extend the pattern). **Architectural milestone:** Sprint 9.2 closes the dead-code loop from Sprint 9.0's discovery audit — the 3 byte-identical `.{prefix}-back-link` CSS rules that were flagged as design-intent-without-markup are now realized as a single canonical `.subpage-header__back` primitive consumed by 6 surfaces (3 vocab modules + 3 speaking mode panels). The pre-9.2 eyebrow → back-link promotion is reusable on any future page that adopts `.subpage-header` (writing-dashboard mode entry, admin index, etc.) without per-page redeclaration. The vocab pushState upgrade also gives browser back/forward a proper history axis through mode visits — a UX improvement that costs zero markup churn.
- Sprint 9.1.1-hotfix (PR #190) — **RED bug fix: vocab modules crashed on mount due to backticks inside HTML template literal comments.** Andy's Vercel preview smoke on PR #189 (Sprint 9.1) surfaced 3 sub-pages failing to mount with `ReferenceError: header is not defined at my-vocab.js:53:25` (my-vocabulary.html, vocabulary.html#flashcards, vocabulary.html#exercises). Root cause: Sprint 9.1's retirement comments inside the 3 vocab modules' `const HTML = /* html */ \`...\`` template literals wrapped CSS class names in backticks (e.g. `<!-- Sprint 9.1 — \`.subpage-header\` primitive (components.css). The pre-9.1 \`.mv-header\` "context bar" ... -->`). Backticks inside a template literal terminate the literal early — JS parser interprets the first backtick (before `.subpage-header`) as the closing of the template, then `.subpage-header` parses as a property access, subsequent backticks reopen/reclose the literal, and somewhere in the broken parse the identifier `header` surfaces unqualified at runtime → `ReferenceError`. Total parity of backticks was even (4 in my-vocab.js, 4 in flashcards.js, 6 in exercises.js) so `node --check` and the existing test pins all passed syntactically — only runtime evaluation of the malformed but parser-acceptable structure raised the ReferenceError. **Atomic 3-item fix:** (1) strip backticks from 5 HTML-comment locations across the 3 modules (my-vocab.js lines 53-54: `\`.subpage-header\`` + `\`.mv-header\`` → no backticks + `chrome→content` arrow spelled out as `chrome to content`; flashcards.js lines 43-44: `\`.subpage-header\`` + `\`.fc-header\``; exercises.js lines 34-35: `\`.subpage-header\`` + `\`.ex-header\``; exercises.js line 62: `\`.mode-card\`` inside a 2nd Sprint 9.1 retirement comment about drill cards adopting the shared primitive). The comment semantics are preserved — backticks were decorative around CSS class names, single-quote-style not needed; bare `.classname` reads cleanly in plain text. (2) NEW sentinel test pin block `Sprint 9.1.1-hotfix — vocab module template literals must not contain backticks inside HTML body` in `frontend/tests/vocab-module-loader.test.mjs` — 3 tests (one per module) that extract the template literal between `const HTML = /* html */ \`` and the closing `\`;` using a non-greedy regex, then assert the captured body contains no backticks. This regression-guard catches the exact bug class via static analysis without needing a DOM/browser; AST-level linting would be heavier and project doesn't yet ship an ES lint config for this. (3) Ledger row + audit hotfix count bump 17 → 18 (Sprint 9.1.1 is a RED bug fix, mirroring the Sprint 6.15.4/5/6/7/8 hotfix accounting). **Frontend gate: 2588/2588 pass** (net +3 vs Sprint 9.1 baseline 2585 — the 3 new hotfix sentinel pins). **Verification:** programmatic backtick count inside template body for all 3 modules is now 0; `node --check` still passes (it always did — parity was even); but runtime evaluation no longer hits the ReferenceError. **Audit gap closed:** Sprint 9.1's pre-push verification ran `grep` for retired CSS class names + ran the full test suite but did NOT smoke-test the actual runtime module-mount paths on the Vercel preview. The Sprint 9.1 pre-push grep checks confirmed CSS structural cleanup but couldn't catch a backtick-in-template-literal runtime bug — that lives entirely in the JS evaluation surface, not the markup audit surface. **Possible Gate 12 candidate** (deferred decision pending Andy): "JavaScript template literal hygiene — HTML comments inside `/* html */` template literals must contain no backticks, even decorative ones around identifiers" — filed against this hotfix as origin. Sentinel pin (3 tests) is the immediate guard; gate formalization is the documentation layer that can land in a future methodology sprint. **Lesson surfaced:** when refactoring across JS template literal boundaries, the temptation to use backticks around CSS/HTML identifiers in inline comments is high (they look natural in Markdown-style prose). The cost is total parse fragility: any backtick inside the literal collapses the surrounding template. The fix is mechanical (drop the backticks) and the sentinel catches it; the deeper lesson is that comment-style decisions inside template literals need different conventions than top-of-file JSDoc — single quotes, asterisks, or just bare text are all safer. **NO scope creep:** Sprint 9.1's CSS lift / 3-page refactor / D3 deletion all preserved byte-identical; only 5 comment locations changed in JS files; no template literal restructuring; no markup changes; no test pin retargets (only added 3 new pins); backend untouched. **Andy testing pattern note:** the Vercel preview smoke caught this — emphasizes value of always smoke testing PR previews before merge, especially when refactors touch runtime-evaluated template surfaces. Recording it here as a process win for future sprints.
- Sprint 9.1 (PR #189) — **vocab sub-pages hard refactor: lift `.subpage-header` + `.mode-card` to components.css.** Andy-approved Option B (hard refactor, 1 sprint) after Sprint 9.0 discovery audit measured ~200px chrome→content visual gap on the 3 vocab sub-pages (my-vocabulary / flashcards / exercises) — chrome's own 64px bottom margin + page-context bar's ~56px height + main's `pt-20` 80px top padding. Andy's "lọt thỏm" perception was mechanically valid, not aesthetic. Sprint 9.1 collapses the gap to ~108px while simultaneously firing the rule-of-three abstraction trigger from Sprint 8.2's architectural note. **Phase D Andy decisions (8):** (Q1) Option B hard refactor 1 sprint (not Option A soft + Sprint 9.2 hard); (Q2) `.subpage-header` lifts to components.css as canonical primitive; (Q3) `.mode-card` lifts to components.css (3rd adopter fires rule-of-three); (Q4) banner action button optional per page (kept "Add word" on my-vocab, dropped from flashcards/exercises — context bar already has "Tạo bộ mới"); (Q5) drop `📚` emoji from flashcards H1 title (canonical title-text-only treatment); (Q6) top-spacing target ~110px chrome→content (preserve "Vocabulary | {title}" page-context anchor on the 3 sub-pages); (Q7) speaking sub-pages out of scope this sprint (deferred to Sprint 9.2 candidate if 9.1 lands clean); (Q8) D3 dead-code CSS cleanup as companion in same patch (4 orphaned rules: `.ex-card.disabled` / `.ex-card.disabled:hover` / `.ex-card__cta--soon` / `.pill-soon`). **CSS lift to components.css (NEW canonical primitives):** (a) `.subpage-header` + `.subpage-header__lhs` + `.subpage-header__sep` + `.subpage-header__title` — flex-row layout (justify-between with `--av-space-4` gap), inner eyebrow + pipe-separator + title typography pattern, `--av-fs-lg` font-size + `--av-fw-semibold` + `--av-tracking-tight` letter-spacing; (b) `.modes-grid` + `.mode-card` outer frame + inner-class skeleton (`.head` / `.icon` / `.arrow` / `h3` / `.lede`) lifted from speaking.css (Sprint 8.1 origin) + vocabulary.css (Sprint 8.2 mirror) — single source of truth eliminates the byte-identical duplication flagged in Sprint 8.2's architectural note; (c) `.mode-card.disabled` + `.mode-card .lock-tag` consolidated into components.css alongside the primitive. **CSS retirement (4 files):** (a) `my-vocabulary.css` — `.mv-header` / `.mv-back-link` / `.mv-header__sep` / `.mv-header__title` rule sets retired (4 rules, byte-identical to canonical); `.mv-back-link` was dead code in the post-Sprint-7.5 module template; (b) `flashcards.css` — `.fc-header` / `.fc-header__sep` / `.fc-header__title` / `.fc-back-link` retired (4 rules); `.stack-card` outer frame + `:hover` + `.disabled` + `.disabled:hover` variants retired (4 rules consumed by `.mode-card`); `.stack-card:hover .delete-btn` flipped to `.mode-card:hover .delete-btn` (single selector update — JS templates now emit `.mode-card`); kept `.stack-icon` / `.stack-name` / `.stack-meta` / `.pill-auto` / `.pill-manual` / `.delete-btn` (stack-specific inner classes); (c) `exercises.css` — `.ex-header` / `.ex-back-link` retired (2 rules); `.ex-card` outer frame + 4 D3-orphan rules (`.ex-card.disabled` / `.ex-card.disabled:hover` / `.ex-card__cta--soon` / `.pill-soon`) + 3 pre-9.1 drill-card inner classes (`.ex-card__title` / `.ex-card__body` / `.ex-card__cta`) all retired (9 rules total, Phase D Q8 companion cleanup); kept `.ex-pill` / `.pill-live` / `.ex-hub-title` / `.ex-hub-sub`; (d) `speaking.css` + `vocabulary.css` — Sprint 8.1 + Sprint 8.2 `.mode-card` + `.modes-grid` blocks retired (replaced by Sprint 9.1 retirement comments pointing at components.css). **JS template refactor (3 files, vocab-modules):** (a) `my-vocab.js` — `HTML` template: replaces `<header class="mv-header">` 4-line markup with canonical `<header class="subpage-header mb-6">` + `<div class="subpage-header__lhs">` wrapping `<p class="eyebrow">Vocabulary</p>` + `<span class="subpage-header__sep">|</span>` + `<h1 class="subpage-header__title">My Vocab Bank</h1>` + the existing `data-action="toggle-add-form"` Add Word button (Phase D Q4 — banner action retained); `<main>` wrapper class `pt-20 pb-6` → `pt-4 pb-8` (Phase D Q6 — top-spacing tightening); (b) `flashcards.js` — same `.subpage-header` pattern, banner action button dropped per Phase D Q4 (context bar's "Tạo bộ mới" suffices); H1 title text "📚 Flashcards" → "Flashcards" per Phase D Q5 emoji-drop decision; `.stack-card` outer class flipped to `.mode-card` in both `autoCard()` + `manualCard()` template literals (preserving `.disabled` modifier branch); `<main>` wrapper class `pt-20 pb-8` → `pt-4 pb-8`; (c) `exercises.js` — same `.subpage-header` pattern, banner action button dropped per Phase D Q4; D3 "Speak with target" `<div data-card="d3">` block (lines 88-99) deleted; `init()` function's `me.d3_enabled` read + `!d3` conditional clause removed (Phase D Q8 companion); drill cards now adopt `.mode-card` outer class with home.css `.head` > `.icon` + pill inner skeleton (Lucide `pencil` / `layers` icons replacing prior emoji); `<main>` wrapper class `pt-20 pb-8` → `pt-4 pb-8`. **Test changes (3 files, all pin flips — no new files):** **(a) `vocab-module-loader.test.mjs`** — 3 stale describe block pins flipped: "HTML template ships the canonical `mv-header` markup" → "subpage-header markup" (+ Add Word banner action retained); "HTML template ships the canonical `fc-header` markup" → "subpage-header markup" (+ 📚 emoji-absent guard per Phase D Q5); "HTML template ships the canonical `ex-header` markup" → "subpage-header markup"; "4 render states + 3 drill cards" → "4 render states + 2 drill cards (D3 retired)"; 3 "must NOT appear in shell" negative pins retargeted from `.{prefix}-header` selectors → `.subpage-header` (sentinel that the canonical primitive is the only header pattern shipped). **(b) `chrome-spacing-canonical.test.mjs`** — 3 module template wrapper pins flipped: my-vocab template `pt-20 pb-6` → `pt-4 pb-8`; flashcards template `pt-20 pb-8` → `pt-4 pb-8`; exercises template `pt-20 pb-8` → `pt-4 pb-8` (all 3 vocab module shells now use the tightened spacing). **(c) `speaking-redesign.test.mjs`** — Sprint 8.1 `describe('Sprint 8.1 — speaking.css mode-card primitive')` block retargeted to `describe('Sprint 9.1 — .mode-card primitive lifted to components.css')` — primitive-existence pins now read from `components.css` (not `speaking.css`); required-classes roster (4-tuple) dropped `.mode-card` + `.modes-grid` entries (primitive no longer co-located with skill-page CSS); NEW 4th test added to the Sprint 9.1 describe block — sentinel asserting `speaking.css no longer redeclares .mode-card / .modes-grid` (regression guard for the lift). **Frontend gate: 2585/2585 pass** (net +1 vs Sprint 8.3 baseline 2584 — Sprint 9.1 added the speaking-side regression-guard pin while pin-flipping in place across the 3 other test files). **Audit hotfix count unchanged at 17** (Sprint 9.1 is a refactor, not audit hotfix or methodology — same accounting as Sprint 8.1 / 8.2 / 8.3 IA refactors). **Pre-push verification:** `grep -nE '^\.(mv-header|fc-header|ex-header|stack-card|ex-card)\b' frontend/css/*.css` → 0 outer-class rules remain (only the `:hover` descendant selector reference flipped to `.mode-card:hover`); `grep -rn "Speak with target" frontend/js/` → 0 production matches (only inline comment retirement marker in `exercises.js`); `grep -rn "Speak with target" frontend/tests/` → 1 negative-pin sentinel in `vocab-module-loader.test.mjs` (asserts the D3 markup is gone). **NO scope creep:** speaking sub-pages NOT touched (Phase D Q7 — Sprint 9.2 candidate if 9.1 lands clean); the broader speaking-redesign test file's other describe blocks untouched; the 4 grammar pages + 6 writing pages + 3 admin pages untouched; backend untouched; chrome component untouched; `theme-toggle-layout-context.test.mjs` — verified no `mv-header` / `fc-header` / `ex-header` pins remain. **Architectural milestone:** Sprint 9.1 is the first sprint to formally execute the rule-of-three CSS abstraction trigger flagged in Sprint 8.2's architectural note (`.mode-card` lifted to components.css after 3rd page adopted the pattern — 3 vocab sub-pages join the prior speaking.html + vocabulary.html landings as adopters via the 5 pages total). The same pattern applies to `.subpage-header` (3 byte-identical `.{prefix}-header` rule sets consolidated to single canonical primitive — net -8 LOC across the 3 vocab module CSS files). Future pages adopting `.mode-card` (e.g., writing-dashboard.html mode entry, admin index, etc.) can consume directly from components.css without any per-page redeclaration; this also forecloses style drift between adopter pages.
- Sprint 8.3 (PR #188) — **grammar.html badge row cleanup.** Andy-requested removal of the sticky `.gw-subnav` editorial breadcrumb at the top of the Grammar Wiki landing page ("Grammar Wiki" label + `<span id="total-articles">` pill + "8 nhóm chủ đề" pill). The row sat between the chrome and the hero, duplicating the chrome's `active="grammar"` skill highlight + the hero title's "Grammar Wiki / IELTS Grammar Reference" editorial framing — net visual tax for zero unique information on the landing page. **Scope discipline: landing page only.** The 4 grammar sub-pages (grammar-article / grammar-compare / grammar-roadmap / grammar-search) keep their `.gw-subnav` breadcrumb — there the badge row carries distinct article-context value (it's the only persistent "you are in the Grammar Wiki" anchor when a user is deep-linked into a leaf article). The CSS rule for `.gw-subnav` in `grammar-wiki.css` is preserved unchanged — it's still consumed by the 4 sub-pages. **Markup changes (grammar.html lines 78-91):** `<nav class="gw-subnav sticky top-0 z-20 border-b border-white/5">` block (12 lines including the 2 child `<span class="ds-badge">` pills and the `#total-articles` id slot) retired, replaced by an 8-line Sprint 8.3 retirement comment that documents the landing-vs-sub-page scope split. **No JS changes required.** `grammar.js:526` already guards the `#total-articles` lookup with `if (totalEl) totalEl.textContent = data.total_articles || 0;` — the population path no-ops gracefully on the landing page while still firing on the 4 sub-pages. **No CSS changes required.** The `.gw-subnav` rule + the `--av-chrome-bottom-margin` token usage stay byte-identical in `grammar-wiki.css` for the sub-pages' continued use; the chrome-spacing-canonical Cat E pin (`grammar-wiki.css — .gw-subnav ships margin-bottom: var(--av-chrome-bottom-margin)`) remains green. **No hero spacing adjustment needed.** The chrome's own `margin-bottom: var(--av-space-16)` (64px Sprint 7.14 contract) + the hero `<div class="py-14">` (56px top padding) provide ~120px clearance from chrome bottom to the hero title — visually identical to the prior layout's chrome → badge-row → hero rhythm minus the badge-row's 48px height + 64px margin-bottom (the hero now sits where the badge row's bottom used to be, give or take a few pixels of token-derived spacing). **Test changes (1 file):** **`grammar-wiki-redesign.test.mjs`** — `grammar.html landing / JS-coupled IDs preserved` describe block: `total-articles` dropped from the `REQUIRED_IDS` roster (it's no longer in the landing markup); inline comment added explaining the Sprint 8.3 landing-vs-sub-page scope split + grammar.js's defensive null guard. NEW Sprint 8.3 retirement sentinel test added under the same describe: negative assertion that `<nav class="gw-subnav">` is absent on the landing page AND that `id="total-articles"` is absent. The 4 sub-pages keep their full `.gw-subnav` markup + `#total-articles` pill — no test changes for those (their roster is in a separate describe block elsewhere in the file, untouched). **Frontend gate: 2584/2584 pass** (zero net change in pin count — 1 ID dropped from REQUIRED_IDS, 1 new Sprint 8.3 retirement-sentinel test added, balanced). **Audit hotfix count unchanged at 17** (Sprint 8.3 is a minor UX cleanup, not audit hotfix or methodology — same accounting as Sprint 8.1 / 8.2 IA refactors). **Pre-push verification:** `grep -n "Grammar Wiki\|gw-subnav\|total-articles" frontend/grammar.html` → remaining matches are the page `<title>`, retirement comments, the hero CTA "Xem 8 nhóm chủ đề" button, the back-link breadcrumb "← Grammar Wiki", and the "Khám phá theo nhóm chủ đề" section header (all intentional / unrelated to the badge row). The `<nav class="gw-subnav">` markup itself is gone. **NO scope creep:** hero block untouched (Phase A surfaced that the hero ALSO carries a `<div class="ds-badge ds-badge-teal">IELTS Grammar Reference</div>` editorial badge at lines 97-103 — this is a separate Sprint 6.19 Phase B Q3 editorial exception, NOT in scope); search box untouched; 3 hero CTAs (`btn-cta` row) untouched; 8 group cards (`#groups-section`) untouched; 4 sub-page badge rows untouched; `grammar.js` untouched; `grammar-wiki.css` untouched.
- Sprint 8.2 (PR #186) — **vocabulary.html IA refactor: tab row → mode cards.** Andy-approved restructure that applies the Sprint 8.1 speaking-page pattern to the Vocabulary landing page. Pattern reuse: same `.mode-card[data-mode]` dashboard grid + `switchTab`-equivalent panel toggle + chrome-reload "back to dashboard" UX, adapted for vocabulary's specifics. **Key structural differences vs Sprint 8.1**: (a) tab handler lives in external `frontend/js/vocab-landing.js` (not inline like speaking.html); (b) original tab row was a full ARIA tablist (`role="tab"` + `role="tabpanel"` + `aria-selected` + `aria-controls`), not a plain `<button>` row; (c) hash-routing was already wired (`vocabulary.html#flashcards` deep-links) and is preserved per Phase B Q5; (d) module lazy-loading via `dynamic-import` + `data-mounted` idempotency guard (Sprint 7.3-7.5 module-mount pattern) is preserved byte-identical. **Phase B Andy decisions (5):** (Q1) leaner card subtitles (proposed Vietnamese one-liners, not the longer existing copy); (Q2) Topic Bank card stays clickable + adds `<span class="lock-tag">Soon</span>` badge inside the card heading (signals coming-soon without disabling the click); (Q3) section heading "Bắt đầu học từ vựng" (Sprint 8.1 pattern + verb form for vocabulary semantic); (Q4) stats strip stays visible above panels + dashboard (not hidden when a panel activates — the 3-stat strip contextualizes vocabulary work across all modes); (Q5) hash routing preserved as-is (deep-linking via `#my-vocab` / `#flashcards` / `#exercises` / `#topic-bank`). **Markup changes (vocabulary.html):** (a) lines 120-142 — entire `<nav class="vocab-tabs" role="tablist">` block + 4 `<button class="tab" role="tab">` children retired (replaced by ~9-line Sprint 8.2 retirement comment); (b) NEW `<section class="vocab-modes" aria-labelledby="modes-heading">` with `<h2 id="modes-heading">Bắt đầu học từ vựng</h2>` + `<div class="modes-grid">` of 4 `<a class="mode-card" data-mode="...">` anchors adopting Sprint 8.1's inner skeleton (`.head` > `.icon` + `.arrow`, `h3`, `.lede`). Lucide icons: `book-open` / `layers` / `dumbbell` / `landmark` (per Phase A proposal table approved Phase B Q1). The Topic Bank card's `<h3>` wraps an inline `<span class="lock-tag">Soon</span>` badge; (c) panel sections — `aria-labelledby` updated from `tab-{name}` (now-deleted tab IDs) to the new `modes-heading` so screen readers still anchor each panel to its trigger surface; `panel-my-vocab` gains the `hidden` attribute (previously was the default-active panel without `hidden`) so all 4 panels start hidden — dashboard view is the new default landing state. **JS changes (vocab-landing.js):** function rename `setupTabs()` → `setupModeCards()` (drops the ArrowLeft/ArrowRight tab-cycle keyboard a11y — no row to cycle through); `activateTab()` drops the `$$('.vocab-tabs .tab').forEach(...)` `.active` class + `aria-selected` + `tabIndex` toggle branch (no buttons exist); adds `const dashboard = $('.vocab-modes'); if (dashboard) dashboard.hidden = true;` to hide the dashboard view when a panel activates. Panel toggle + module-mount + skeleton-render + retry-on-error + hashchange listener all preserved byte-identical. `bootstrap()` modified: NO longer auto-activates `DEFAULT_TAB` on cold load; instead, only activates a panel when `window.location.hash` matches a `VALID_TABS` entry. Header comment block updated to document the Sprint 8.2 architecture change. **CSS changes (vocabulary.css):** (a) `.vocab-tabs` + `.tab` + `.tab .ico` + `.tab.active` + `.tab.active::after` + `.tab:focus-visible` + `.tab[disabled]` + `.tab[disabled]::after` + `.tab .lock-tag` rules retired (~70 LOC, replaced by retirement comment); (b) NEW `.vocab-modes` section + `.vocab-modes h2` + `.modes-grid` (auto-fit minmax(260px, 1fr)) + `.mode-card` primitive with inner-class rules (`.head` / `.icon` / `.arrow` / `h3` / `.lede`) — mirrors Sprint 8.1 speaking.css block, using canonical `--av-*` tokens only. `.mode-card .arrow` uses `--av-text-muted` per the `--av-text-faint` cap discipline. NEW `.mode-card .lock-tag` rule (small badge inside h3, preserves the visual treatment from the retired `.tab .lock-tag`); (c) `.tab-panel` + `.tab-panel[hidden]` rules preserved — still used by activateTab() panel toggle. **Test changes (2 files):** **(a) `vocab-landing.test.js`** — full rewrite of the DOM shim + tests: `buildPage()` no longer constructs the 4 tab-button DOM (`tabs` array removed); instead constructs a `dashboard` `.vocab-modes` element + 4 `.mode-card[data-mode]` anchors + 4 panels (all hidden by default). `document.querySelectorAll('.vocab-tabs .tab')` selector replaced with `'.mode-card[data-mode]'`; `document.querySelector('.vocab-modes')` added for dashboard lookup. activateTab assertions retargeted: panel.hidden toggle + dashboard.hidden = true (instead of tab.classList.active + aria-selected). NEW dashboard-visible-on-cold-start sanity assertion. topic-bank test extended: dashboard hides even when target mode has no module loader. Unknown-mode-fallback test retargeted to panel.hidden assertion. `DEFAULT_TAB` comment clarified: kept for unknown-mode fallback path only, no longer auto-activated on cold load. **(b) `vocabulary-redesign.test.mjs`** — Sprint 6.0 iframe contract describe block renamed to "Sprint 8.2 mode-card IA + module-mount contract". 4 `tab-{name}` ID pins flipped to "must NOT exist" guards. 4 `data-tab` pins flipped to `data-panel` pins. NEW 4 `.mode-card[data-mode]` markup pins. NEW `.vocab-modes` section + `modes-heading` + `.modes-grid` wrapper pin. NEW Topic Bank `.lock-tag` Soon badge pin. ARIA pin updated: `role="tab"` count 4 → 0; `aria-selected` count 4 → 0; `role="tabpanel"` count stays 4. NEW "all 4 panels start hidden" assertion (dashboard is default landing state). Functional-microcopy describe block rewritten: tab-icon emoji (📚 🔄 ✏︎ ✸) audit retired; replaced with Lucide icon roster pin (book-open / layers / dumbbell / landmark) + reverse emoji-absent guard inside the modes section. Vietnamese tab-label pins retired; replaced with mode-card title pins (English-cased per Phase B Q1) + lede subtitle pins (4 Vietnamese one-liners). **Frontend gate: 2584/2584 pass** (net +29 pins vs Sprint 8.1 baseline 2555 — Sprint 8.2 added 4 mode-card data-panel pins + 4 mode-card data-mode pins + 4 mode-card Lucide pins + 4 mode-card title pins + 4 mode-card lede pins + 4 panel-hidden pins + 4 retired-tab-id pins + 3 section/heading/grid pins + 1 Topic Bank Soon badge pin + 1 ARIA count assertion + 1 dashboard-default-landing assertion; retired the 4 stale tab-id + 4 data-tab + 1 aria-controls pins). **Audit hotfix count unchanged at 17** (Sprint 8.2 is an IA refactor, not audit hotfix or methodology). **Pre-push verification:** `grep -nE 'role="tablist"|class="vocab-tabs"' frontend/pages/vocabulary.html` → 0 matches (tab-row markup fully retired). **NO scope creep:** Topic Bank Vocab Bank feature NOT activated (Phase 5+ separate sprint per Phase A Step 4); module loaders + `_loader.js` + skeleton render contracts untouched; my-vocabulary.html / flashcards.html / exercises.html standalone sub-pages untouched (Sprint 7.3-7.5 module pattern preserved); backend untouched; chrome component untouched; stats strip untouched (Phase B Q4 — always visible). **Architectural note:** Sprint 8.2 marks the second page adopting the `.mode-card` pattern (after Sprint 8.1 speaking.html). CSS rules currently live in both speaking.css + vocabulary.css with byte-identical bodies. Lifting `.mode-card` / `.modes-grid` / inner-class rules to a shared location (components.css or a new modes.css) becomes warranted if a 3rd page adopts the pattern — per the "rule of three" abstraction discipline. Re-clicking the chrome "Vocabulary" nav link (`/pages/vocabulary.html`) reloads the page to the dashboard view (no hash → no panel activation) — same "back to dashboard" UX as Sprint 8.1.
- Sprint 8.1 (PR #185) — **speaking.html IA refactor: tab row → mode cards.** Andy-approved restructure of the Speaking page's primary entry surface. **Phase A audit surprise**: dashboard view already shipped 3 quick-start `.part-card` divs at speaking.html:229-291 each targeting `switchMainTab('practice'|'partbpart'|'fulltest')` — so the actual refactor became: (1) delete the redundant `.main-tab-nav` tab row, (2) delete the `#continue-cta` "Tiếp tục luyện tập" banner + its JS show/hide logic, (3) replace the existing 3 `.part-card` divs with 3 home.css-style `.mode-card` anchors, (4) rewire mode entry through `data-mode` event delegation (replacing the `onclick="switchMainTab('...')"` direct invocations). **Phase B Andy decisions (3):** leaner card subtitles (≤ 80 chars each, not the longer existing copy); keep `#dashboard-empty` "Luyện tập ngay →" button (still works as the empty-state primary action); section heading text "Bắt đầu luyện tập" (reuse existing copy — less churn than "Chế độ luyện tập"). **Markup changes (speaking.html):** (a) lines 96-131 — entire `<nav class="main-tab-nav">` block + Sprint 6.16 cleanup comment retired (replaced by ~6-line Sprint 8.1 retirement comment); (b) lines 180-186 — `#continue-cta` banner with `#cta-topic-label` + `#continue-cta-link` retired; (c) lines 229-291 — 3 `.part-card` divs replaced with `<section class="speaking-modes" aria-labelledby="modes-heading">` wrapping a `<div class="modes-grid">` of 3 `<a class="mode-card" data-mode="practice|partbpart|fulltest">` anchors. Each card adopts home.css `.skill-card` inner skeleton (`.head` > `.icon` + `.arrow`, `h3`, `.lede`) with Lucide icons (`target` / `list-checks` / `trophy`) — visual language matches home.html's "Kỹ năng IELTS" grid for cross-page parity. **JS changes (speaking.html inline):** `switchMainTab(tab)` trimmed — `#mtab-{tab}` button-toggle branch removed (no buttons to toggle); panel-toggle branch + lazy-load branches preserved. NEW Sprint 8.1 mode-card click delegation block: `DOMContentLoaded` handler binds `card.addEventListener('click', e => { e.preventDefault(); switchMainTab(card.dataset.mode); })` on every `.mode-card[data-mode]`. `renderStats()` `#continue-cta` show/hide block (~30 LOC) deleted. `applyPermissions()` extended: `.mode-card[data-mode="practice"]` added to `practice_single` gate, NEW `.mode-card[data-mode="partbpart"]` selector for `practice_part` gate (previously only enforced via panel-locked banner), `.mode-card[data-mode="fulltest"]` added to `practice_full` gate — regression-prevention for visual permission state on the new entry surface. **CSS changes (speaking.css):** (a) `.part-card` rule retired (no longer used); (b) `.main-tab-nav` / `.main-tab-btn` / `.main-tab-label` / `.main-tab-sub` rules retired (~70 LOC, replaced by retirement comment); (c) `.main-tab-panel` rule preserved (the 4 `#tab-{name}` panels still toggle via `switchMainTab`); (d) NEW `.modes-grid` (auto-fit minmax(260px, 1fr), gap --av-space-4) + `.mode-card` (var(--av-surface-card) bg, var(--av-border-subtle) border, var(--av-radius-lg)) + inner-class skeleton rules (`.head` / `.icon` / `.arrow` / `h3` / `.lede`) — `.arrow` color uses `--av-text-muted` (not home.css's `--av-text-faint`) per speaking.css's § 17.6 `--av-text-faint` cap discipline (the faint token is reserved on this page for placeholder rules). **Test changes (3 files):** **(a) `speaking-redesign.test.mjs`** — stale pins flipped (4 `mtab-*` button pins → "tab row retired" sentinel, `continue-cta`/`cta-topic-label`/`continue-cta-link` pins → "banner retired" sentinel, `.main-tab-btn` padding/nowrap pin retired, `main-tab-nav` emoji + Lucide audit retired, the "3 quick-access cards use Lucide" test retargeted to `.mode-card` Lucide audit). Required-classes roster updated: `-.part-card`, `-.main-tab-nav`, `-.main-tab-btn`, `-.main-tab-label`, `-.main-tab-sub`; `+.mode-card`, `+.modes-grid` (`.main-tab-panel` kept). NEW Sprint 8.1 sentinel describe block (3 sub-blocks, 11 pins): mode-card markup (3 data-mode anchors + section heading + grid wrapper), JS delegation (querySelectorAll + dispatch), switchMainTab trim (no `#mtab-*` lookup, still toggles `#tab-*`), default landing state (`#tab-dashboard.active`), permission-gating selector extension (all 3 data-mode entries), CSS primitive audit (`.mode-card` inner skeleton + tokens), retired-surface guards. **(b) `chrome-spacing-canonical.test.mjs`** — Sprint 6.18 Cat F describe block: `.main-tab-nav margin-bottom` pin retired (the 64px chrome-to-content rhythm is now enforced entirely by the chrome component's own bottom margin since speaking.html no longer has a secondary nav between chrome and content). `onboarding.html primary <main> pt-20` pin kept. **(c) `subheading-pattern-canonical.test.mjs`** — Sprint 6.19 Phase B Q4 "practice modes preserved as distinct patterns" describe block: `.main-tab-nav` 4-tab pin flipped to 3 `.mode-card[data-mode]` pins; `grammar.html .btn-cta` pin unchanged (preserves the Phase B Q4 intent that Speaking entry pattern is distinct from grammar's editorial hero CTA). **(d) `speaking-ia-cleanup.test.mjs`** — full rewrite: Sprint 6.16 framing ("no cross-skill links in main-tab-nav") retargeted at the new `.speaking-modes` section ("no cross-skill mode-cards" — same intent, new surface). Old `mainTabNavBlock` extraction replaced with `modesSection` extraction; 4 internal-tab pins flipped to 3 mode-card pins; Sprint 6.16 marker pin retargeted to Sprint 8.1 marker pin; cross-skill-link sentinels (Grammar Wiki + Từ vựng + ../grammar.html + /pages/vocabulary.html) updated for the new surface; JS contract preservation pins (`switchMainTab`, `applyVocabBankFlag` / `applyExercisesFlag` / `applyFlashcardsFlag` dormant feature-flag stubs) kept. **Frontend gate: 2555/2555 pass** (zero failures; net +7 pins vs Sprint 7.14 baseline 2548 — Sprint 8.1 added 11 mode-card pins, retired 4 stale-pin describe blocks for net +7). **Audit hotfix count unchanged at 17** (Sprint 8.1 is an IA refactor, not audit hotfix or methodology). **Pre-push verification:** `grep -nE "<nav[^>]*main-tab-nav|Tiếp tục luyện tập" frontend/pages/speaking.html` → only matches the Sprint 8.1 retirement comment (no markup remains). **NO scope creep:** mode section panel internals (Luyện tập / Luyện từng Part / Full Test) untouched; backend untouched; no deep-link hash routing added (separate sprint candidate); chrome component untouched; mode card stats / personalization deferred to Sprint 8.2+. **Architectural note:** Sprint 8.1's `.mode-card` pattern is reusable for future skill-page IA refactors. If Andy wants to apply the same "dashboard + mode-cards, no tab row" treatment to writing-dashboard.html / vocabulary.html / grammar.html, those sprints can adopt this template directly. Re-clicking the chrome "Speaking" nav link (`/pages/speaking.html`) reloads the page to the dashboard view — no extra UI required for "back to dashboard" per Phase B Q5.
- Sprint 7.14 (PR #184) — **🎯 DEBT-CHROME-ARCHITECTURE CLOSED — chrome unification cleanup sprint.** Scope split across three axes: (a) retire the duplicate chrome CSS now that all 18 pages consume `<aver-chrome>`; (b) formalize Gate 11 (the embedded-host resource-parity gate filed informally in Sprint 7.7-hotfix + 7.8-hotfix and promoted to candidacy in Sprint 7.13.1-hotfix); (c) resolve 5 pre-existing test pin staleness failures inherited from Sprint 7.4/7.5 + Sprint 6.16.1 + Sprint 7.14's own CSS retirement. **(a) Chrome CSS retirement (4 files).** `frontend/css/aver-design/components.css` — `.topnav` / `.topnav-wrap` / `.brand` / `.nav-links` / `.topnav-right` / `.user-pill` / `.user-menu` / `.user-menu-dropdown` / `.user-menu-item` / `.av-theme-toggle` + `[data-theme="dark"]` icon-swap rules retired (~224 LOC deleted, replaced by 2 retirement comment blocks). `.shell` primitive preserved (still owns page-level layout). `frontend/css/home.css` — chrome block + mobile `@media` chrome lines retired (~100 LOC). `frontend/css/vocabulary.css` — `.topnav` / `.nav-links` / `.topnav-right` block + mobile breakpoint chrome lines retired (~75 LOC). `frontend/css/grammar-wiki.css` — Sprint 7.8-hotfix `body.av-page .topnav-wrap, .topnav, .brand, .nav-links, .topnav-right { font-family: var(--av-font-sans); }` defensive scoping rule retired (now a no-op since chrome moved into shadow root; the editorial DM Sans body font can no longer cascade into chrome). Single source of truth for chrome rules is now the shadow-root `<style>` block inside `frontend/js/components/aver-chrome.js`. **(b) Gate 11 formalization (`DESIGN_SYSTEM.md` § 17.15).** NEW Gate 11 — "Embedded-host resource parity + chrome behavioral contract" — formalized: origin documents the three sprints that filed the same blind-spot class (Sprint 7.7-hotfix vocab modules unstyled when embedded due to missing host Tailwind import, Sprint 7.8-hotfix user pill stuck "..." on 3 pages where bootstrap didn't call `populateUserPill()`, Sprint 7.13.1-hotfix `<aver-chrome>` polled `window.getSupabase` on 5 grammar pages that never called `initSupabase()`). Verification protocol: per-page resource sentinel pins every host-bootstrapped global the component polls; component source must declare its host-resource requirements as a comment block; component must emit a console error naming the missing global *and* the canonical bootstrap call when the polled global is absent after the polling ceiling; defensive-no-op audit must precede adopting a non-defensive consumer of a shared helper. § 17.12 evolution table extended 6 → 7 blind-spot rows (Sprint 7.13.1-hotfix row added). § 17.12 intro updated "six sprints, six distinct mechanisms" → "seven sprints, seven distinct mechanisms". § 17.12 pattern principle list gained Gate 11 entry. § 17.13 consolidation table gained Gate 11 row formalized Sprint 7.14; cumulative gate count 13 → 14. Methodology sections index gained § 17.15 entry. **(c) Test pin staleness resolved (cumulative 14 pin flips + 2 retirement comments + 1 audit count assertion retirement).** `chrome-spacing-canonical.test.mjs` — Sprint 6.18 foundation describe block split: `.shell` pin stays in components.css; `.topnav margin-bottom: var(--av-space-16)` pin moved to aver-chrome.js shadow style. Sprint 6.18 Cat A roster shrunk 4 → 2 (flashcards.html + exercises.html dropped — their wrappers moved into module templates Sprint 7.4/7.5); NEW Sprint 7.4/7.5 sentinel describe block added with module-template `pt-20 pb-8` pins mirroring the Sprint 7.3 my-vocab pattern. `subheading-pattern-canonical.test.mjs` — Sprint 6.19 per-page eyebrow roster shrunk 16 → 14 (same 2 pages dropped); NEW Sprint 7.4/7.5 sentinel describe block added with module-template `<p class="eyebrow">Vocabulary</p>` pins. `chrome-unification-canonical.test.mjs` — Sprint 6.17 foundation block retargeted: `.shell` pin stays in components.css; `.topnav-wrap` / `.topnav` / `.brand` / `.nav-links` / `.topnav-right` / `.user-pill` / `.user-menu` / `.user-menu-dropdown` / `.user-menu-item` / locked::after / mobile breakpoint pins all moved to aver-chrome.js shadow style. Sprint 7.8-hotfix `.brand` consistency block: grammar-wiki.css test inverted (now asserts the defensive `body.av-page .topnav-wrap, .topnav, .brand` font-family override is GONE — Sprint 7.14 retired it as a no-op); components.css `.brand` pin retargeted to aver-chrome.js shadow style. Sprint 7.12 "legacy chrome pages (13 sub-pages) still inline" describe block (already dead post Sprint 7.13 since `CANONICAL_CHROME_PAGES = []`) replaced with Sprint 7.14 closure sentinel pinning (i) components.css no longer redeclares any chrome selector, (ii) user-pill.js + theme-toggle.js still export `canonicalInitials` / `bindToggleButton` (consumed by `<aver-chrome>` via ES module import). `theme-toggle-icon-canonical.test.mjs` — canonical visibility-swap describe block retargeted: all four pins (`.icon-sun` / `.icon-moon` × light / dark) now read from aver-chrome.js shadow style; dark-theme pin pattern flipped `[data-theme="dark"]` descendant → `:host-context([data-theme="dark"])` per Sprint 7.11 Shadow DOM cross-boundary mechanism. `theme-toggle.test.mjs` — "components.css defines .av-theme-toggle" pin retargeted to aver-chrome.js shadow style. `gate-9-5-9-6-9-7-formalization.test.mjs` — "12 audit gates cumulative metric documented" + "Audit hotfix count remains 13 (unchanged by Sprint 6.16.1)" snapshot-in-time assertions retired (live counts float forward in phase-closure-ledger.test.mjs which already pins the current values). `gate-10-formalization.test.mjs` — Sprint 6.20 intro pin "six sprints, six distinct mechanisms" → "seven sprints, seven distinct mechanisms"; "§ 17.13 header mentions cumulative 13 gates" → "cumulative 14 gates"; NEW Sprint 7.14 "§ 17.15 Gate 11 formalization" describe block added (7 pins: § 17.15 existence + Gate 11 reference + origin sprints + verification protocol + anti-patterns + § 17.13 table row + § 17.12 evolution row + § 17.12 pattern principle row). **Frontend gate: 2548/2548 pass** (zero failures; net +8 pins from Sprint 7.13.1-hotfix baseline 2540/2545). **Audit hotfix count unchanged at 17** (Sprint 7.14 is a planned closure sprint — not audit hotfix; it was always part of the chrome unification roadmap declared as far back as Sprint 7.10's Phase B approval). **DEBT-CHROME-ARCHITECTURE — CLOSED.** The chrome contract is now end-to-end consolidated: 18 host pages each carry 2 lines of chrome reference; chrome markup + chrome CSS + chrome behavior all live in one component file (`frontend/js/components/aver-chrome.js`); the cross-page anchor sentinel (Gate 10) + the embedded-host resource parity sentinel (Gate 11) jointly enforce the contract at CI. Any future chrome drift (markup, CSS, runtime resource) fails immediately, no manual smoke required. **Pre-push verification:** `grep -rn "^\.topnav\|^\.brand\|^\.nav-links\|^\.av-theme-toggle\|^\.user-pill\|^\.user-menu" frontend/css/` → 0 matches (chrome rules fully retired from CSS files); remaining test-side references to `components.css` × `topnav` / `brand` / `user-pill` are all Sprint 7.14 guard sentinels or comment references — no active pins still expect chrome rules to live in components.css.
- Sprint 7.13.1-hotfix (PR #183) — **RED chrome regression: user pill stuck on placeholder across all 5 grammar pages post Sprint 7.13.** Andy reported grammar.html user pill never populates with display name + avatar initials — stays at the HTML default `…` / `·` indefinitely. Andy's per-page audit (`grep -c "initSupabase"` across all 18 migrated chrome pages) surfaced the omission: **5 grammar pages ship `<script src="…supabase-js@2">` + `<script src="…/api.js">` BUT never call `initSupabase(SUPABASE_URL, SUPABASE_ANON)`** — the canonical bootstrap that exposes `window.getSupabase` on the global. Confirmed pages: `frontend/grammar.html`, `frontend/pages/grammar-roadmap.html`, `frontend/pages/grammar-article.html`, `frontend/pages/grammar-search.html`, `frontend/pages/grammar-compare.html`. The 13 other migrated chrome pages all have the full init pattern (12 in `frontend/pages/` + `frontend/onboarding.html` at root). **Root cause:** the Sprint 7.11-built `<aver-chrome>` component polls `window.getSupabase` every 50ms × 60 tries (~3s ceiling) on `connectedCallback`. When `initSupabase()` never runs, `window.getSupabase` stays undefined; the poll exhausts its tries and logs `[aver-chrome] window.getSupabase unavailable after polling; pill placeholder retained.` — pill defaults persist. The bug is **pre-existing on the grammar cluster** since Sprint 6.15 (when the editorial pages first received chrome) — but it was masked because the legacy chrome's `/js/user-pill.js` `populateUserPill()` ran the same getSupabase check defensively and **silently no-op'd if Supabase wasn't initialized** (early-return at `if (!sb || !sb.auth || typeof sb.auth.getSession !== 'function') return;`). The pill placeholder stayed visible in production for an unknown duration without surfacing the underlying config gap. Sprint 7.12/7.13 chrome migration surfaced the latent bug only because the component logs a warning on poll exhaustion (where the legacy module did not) — making the missing-init state observable. **Atomic 5-page fix:** Python script regex-matched each grammar page's `<script src="…/api.js"></script>` line + injected the canonical init block immediately after (copied verbatim from `frontend/pages/home.html` lines 147-152, including the exact SUPABASE_URL `https://nqhrtqspznepmveyurzm.supabase.co` + SUPABASE_ANON `sb_publishable_a_vDrA0c3mT-QlASPW7yhw_YZnUsfT4`). The injected block carries a Sprint 7.13.1-hotfix marker comment explaining the contract (`<aver-chrome>`'s `window.getSupabase` poll resolves once init runs). Every page reported `api.js match count = 1` confirming clean replacement. **NEW sentinel pin** in `chrome-unification-canonical.test.mjs` (~20 pins added under describe block "Sprint 7.13.1-hotfix — every `<aver-chrome>` page must call initSupabase()"): per-page assertion `\binitSupabase\s*\(` regex on all 18 `MIGRATED_PAGES` entries + 1 canonical-URL sentinel asserting `https://nqhrtqspznepmveyurzm.supabase.co` appears in each migrated page (catches a page being pointed at a different Supabase project, a different drift class). Sentinel locks the post-fix state so any future migrated page that ships supabase-js + api.js without `initSupabase()` fails CI. **Gate 11 candidacy formalized in this hotfix:** Codex visual audits + Sprint 6.17 → 6.20 chrome contract sweeps verified markup parity (`.topnav-wrap` + `.brand` + `.user-pill` IDs present) and CSS rule integrity (foundation tokens + canonical chrome rules in `components.css`). **None verified that the page's `<script>` initialization order actually exposes `window.getSupabase` before the component mounts.** The same class of gap also explains why Sprint 7.7-hotfix (vocab modules unstyled — missing Tailwind import on parent) + this hotfix slipped: markup contract ≠ runtime resource contract. **Gate 11 — "Embedded-host resource parity + chrome contract verification":** every page hosting a chrome-dependent component must (a) load the canonical script-set in the canonical order; (b) call the canonical init bootstrap; (c) expose the global the component contracts against. Filed informally in Sprint 7.7-hotfix + Sprint 7.8-hotfix; promoted to formal gate candidacy in this hotfix. **Sprint 7.14 will formalize Gate 11** in `DESIGN_SYSTEM.md § 17`. **Test result:** 182/182 in `chrome-unification-canonical` (+19 net Sprint 7.13.1 pins). Full frontend gate: 2540/2545 pass — same 5 pre-existing failures from Sprint 7.13 baseline (chrome-spacing 6.18 Cat A flashcards/exercises wrapper padding + subheading 6.19 flashcards/exercises eyebrow + 6.16.1 stale audit count assertion) — all scoped to Sprint 7.14 cleanup per the original Sprint 7.13 prompt anti-pattern. **Audit hotfix count 16 → 17** (Sprint 7.13.1-hotfix IS audit hotfix — RED chrome regression that escaped Sprint 7.12 + 7.13 phased migration's smoke). **Honest retrospection:** the missing-init gap pre-existed on grammar pages since Sprint 6.15 chrome adoption — Sprint 7.12 + 7.13 phased migration's smoke (per the prompts' Phase D checklists) called for "user pill populates with Supabase user data (after login)" verification but evidently the manual checks didn't catch grammar pages specifically. The component's defensive polling masked the failure mode as "pill stays placeholder" rather than a hard error, which is the kindest possible failure but also the most silent. Gate 11 candidacy + the new initSupabase sentinel close this class of bug at the markup-audit layer: any future migrated page that ships supabase-js + api.js without `initSupabase()` will fail CI immediately, no manual smoke required. **Pre-push verification:** `grep -rn "SUPABASE_URL\|SUPABASE_ANON" .github/` → no matches (no CI references to lift). **Sprint 7.14 scope updated to include:** retiring `components.css` chrome rules (original scope) + Gate 11 formalization (Sprint 7.13.1-hotfix candidacy promotion) + fixing 5 pre-existing test pin staleness issues (flashcards/exercises wrapper padding + eyebrow pins post Sprint 7.4/7.5 vocab module migration + 6.16.1 audit count assertion bumped to current 17).
- Sprint 7.13 (PR #182) — **🎯 MILESTONE — ALL 18 canonical chrome pages now consume `<aver-chrome>`. Chrome migration batch 2/2 — 13 sub-pages migrated.** Phase 4/5 of the chrome unification refactor authorized by Sprint 7.10 approval. Sprint 7.11 (PR #180) built the additive component; Sprint 7.12 (PR #181) migrated batch 1 (5 skill landings); Sprint 7.13 closes batch 2; Sprint 7.14 retires `components.css` chrome rules + per-page CSS duplicates. **Atomic per-page migrations (13):** Speaking sub-pages — `practice.html` / `result.html` / `full-test-result.html` (`active="speaking"`); Writing — `writing-result.html` (`active="writing"`); Vocabulary sub-pages — `my-vocabulary.html` / `flashcards.html` / `exercises.html` (`active="vocabulary"`); Grammar sub-pages — `grammar-roadmap.html` / `grammar-article.html` / `grammar-search.html` / `grammar-compare.html` (`active="grammar"`); Other — `profile.html` / `onboarding.html` (`active="home"` — IA parent for non-skill pages). **Executed via Python sed-style script** for mechanical consistency: regex matched `<div class="topnav-wrap">…</div>` block + the canonical `<script type="module">import { bindToggleButton } from '/js/theme-toggle.js'; bindToggleButton(...);</script>` + `<script type="module" src="/js/user-pill.js"></script>` script tags; replaced with single-line `<aver-chrome active="…"></aver-chrome>` + `<script type="module" src="/js/components/aver-chrome.js"></script>` in `<head>`. All 13 pages migrated in one mechanical pass — each match returned `chrome=1, scripts=1`, confirming no false-positives or missed boundaries. **Special handling — `profile.html` `renderProfile()` refactor**: the page's inline JS poked `document.getElementById('header-initials').textContent = initials;` (2 sites — `renderProfile` line 282 + `saveProfile` line 347). `header-initials` was a span nested inside the chrome's user-pill avatar — now in shadow root. Both sites refactored to call `document.querySelector('aver-chrome').setUser({ name, initials })` (Sprint 7.12 speaking.html pattern reapplied). The chrome's `setUser({ initials })` overrides the auto-fetch path with explicit initials, preserving profile.html's permissions-context contract via the typed API instead of cross-shadow DOM-poking. **Special handling — speaking sub-pages chart-refresh observer**: speaking.html's chart-refresh `MutationObserver` (preserved Sprint 7.12) is unchanged by this sprint; result.html / full-test-result.html / writing-result.html don't carry chart observers — only theme-toggle binding scripts were retired. **Special handling — grammar sub-pages editorial typography**: DM Sans + Lora § 14.2 sub-system unaffected. The component's Shadow DOM encapsulates chrome typography (Plus Jakarta Sans via `--av-font-sans`); body content keeps editorial fonts via `grammar-wiki.css body.av-page { font-family: 'DM Sans' }`. Sprint 7.8-hotfix's `body.av-page .topnav-wrap, .topnav, .brand, .nav-links, .topnav-right { font-family: var(--av-font-sans); }` defensive rule in grammar-wiki.css becomes a no-op (those selectors no longer match anything in page DOM) but stays through Sprint 7.13 — Sprint 7.14 will retire it alongside the components.css chrome retirement. **Special handling — vocab module shells** (`my-vocabulary.html` / `flashcards.html` / `exercises.html`): the Sprint 7.3-7.5 thin shells already minimized page body to `<main id="mount">`. Sprint 7.13 chrome migration further compresses these shells — they now ship anti-flash IIFE + foundation CSS + Lucide CDN + aver-chrome script + `<aver-chrome active="vocabulary">` + module mount script. The Sprint 7.7-hotfix Tailwind CDN + per-module CSS imports for embedded mode in `vocabulary.html` are unaffected (separate ownership concern). **Special handling — onboarding wizard**: onboarding.html retains its wizard step bar (page-content secondary nav) below the component. `active="home"` selected because onboarding precedes any skill choice — Trang chủ is the IA root. **Test sentinel reshaping (10 files):** **(a) EXTENDED** `chrome-unification-canonical.test.mjs` — `MIGRATED_PAGES` grew from 5 → 18 (added 13 Sprint 7.13 batch records); `CANONICAL_CHROME_PAGES` is now an **empty array** — the inline-chrome roster has fully retired. The "Sprint 7.12 — legacy chrome pages (13 sub-pages) still inline" guard block naturally becomes a no-op (zero pages to iterate) while documenting the Sprint 7.13 milestone. Sprint 6.17 Phase C1 per-page chrome contract pins + Sprint 6.20 Gate 10 `.topnav-wrap` direct-body-child sentinel iterate over empty roster (zero asserts), preserving the structural contract documentation for any future page that adopts inline chrome. **(b) MODIFIED** `chrome-spacing-canonical.test.mjs` — Sprint 6.18 ROSTER (18 pages) describe block now asserts `<aver-chrome active=…>` on every entry (the `MIGRATED_TO_AVER_CHROME` Set guard from Sprint 7.12 retired; all 18 are migrated). **(c) MODIFIED** `theme-toggle-icon-canonical.test.mjs` — `REDESIGNED_PAGES` roster shrunk from 24 → 11 (dropped 13 newly migrated pages). Remaining 11 pages = 2 marketing (`index.html` / `pricing.html`) + 9 admin (Cat 5 out-of-scope for chrome unification per Phase B Q7). **(d) MODIFIED** `theme-toggle-layout-context.test.mjs` — coverage roster threshold lowered 24 → 11; "Grammar Wiki cluster — Sprint 6.15.7-hotfix nav wrapper" describe block flipped from "absolute /js/theme-toggle.js import" to "`<aver-chrome active="grammar">` migration" for all 5 grammar pages; SUB_PAGES block (which pinned toggle inside `.topnav-right` flex wrapper for 4 grammar sub-pages) retired (chrome lives in shadow root; layout-context structural requirement enforced inside the component's shadow tree). **(e) MODIFIED** `practice-redesign.test.mjs` — "header includes the theme toggle button" + "binds the toggle via /js/theme-toggle.js bindToggleButton import" tests replaced with "Sprint 7.13 — chrome migrated to `<aver-chrome active="speaking">`" + "Sprint 7.13 — inline chrome markup + bindToggleButton import retired" guards. **(f) MODIFIED** `result-redesign.test.mjs` — same flip pattern. **(g) MODIFIED** `full-test-result-redesign.test.mjs` — same flip; the only chrome-related test was "header has theme toggle button" (one line). **(h) MODIFIED** `writing-result-redesign.test.mjs` — same flip pattern. **(i) MODIFIED** `profile-redesign.test.mjs` — chrome theme-toggle + .icon-sun/.icon-moon + bindToggleButton + Sprint 6.17 canonical chrome describe tests consolidated into "Sprint 7.13 — chrome migrated to `<aver-chrome active="home">`" + "Sprint 7.13 — inline chrome retired" + "lucide hydration still runs for body-content icons". `header-initials` removed from `requiredIds` array (was inside chrome). **(j) MODIFIED** `onboarding-redesign.test.mjs` — same flip pattern. **(k) MODIFIED** `grammar-wiki-redesign.test.mjs` — "canonical .icon-sun + .icon-moon theme toggle present" + "theme-toggle.js bindToggleButton wired" tests flipped from per-page `if (name === 'landing') return;` Sprint 7.12 gate (which whitelisted the landing migration but kept the 4 sub-pages on inline chrome) to "chrome migrated to `<aver-chrome active="grammar">`" + "bindToggleButton — moved into `<aver-chrome>`" for ALL 5 grammar pages. **(l) MODIFIED** `vocab-module-loader.test.mjs` — "canonical chrome preserved (Sprint 6.17.1 / 6.20)" tests on the 3 thin shells (my-vocabulary / flashcards / exercises) flipped from pinning inline `.topnav-wrap` + `.av-theme-toggle` + `#user-pill` to "chrome migrated to `<aver-chrome>` (Sprint 7.13)" pinning `<aver-chrome active="vocabulary">` + the component script tag. (`replace_all: true` was used since the 3 shells had identical chrome-preservation blocks.) **(m) MODIFIED** `cross-page-navigation-canonical.test.mjs` — "Canonical Trang chủ label present on home-bound pages" HOME_BOUND_SAMPLES roster shrunk from 4 → 1 (only `admin-writing.html` remains on inline chrome; the other 3 — `grammar-article.html` / `profile.html` / `grammar.html` — moved chrome into shadow root, so their "Trang chủ" link is pinned once inside the component source). `HREF_PINS` array shrunk: removed `grammar-article.html` + `grammar-compare.html` + `profile.html` rows (all 3 referenced chrome-internal `/pages/home.html` hrefs now in shadow root); kept practice/result/full-test-result `/pages/speaking.html`, writing-result `/writing/dashboard`, admin-writing `/pages/home.html`, admin.html `pages/home.html` (page-content back-links OUTSIDE chrome). **Frontend gate: 2541/2546 pass** (5 failures remaining are pre-existing on `main` — verified via comparison test on Sprint 7.12 PR review: chrome-spacing 6.18 Cat A `flashcards.html` + `exercises.html` `<main>` `pt-20 pb-8` wrapper pins fail because Sprint 7.4/7.5 vocab module migration moved body templates into JS modules — the test assertions on the HTML shell are stale post Sprint 7.4/7.5 but were not retired; subheading-pattern 6.19 `flashcards.html` + `exercises.html` `<p class="eyebrow">Vocabulary</p>` pins fail for the same reason; PHASE_CLOSURE_LEDGER 6.16.1 `Audit hotfix count remains 13 (unchanged by Sprint 6.16.1)` assertion is stale — count has been 16 since Sprint 7.8-hotfix and the test pin was never updated. **All 5 pre-existing failures are scoped to Sprint 7.14 cleanup** per the Sprint 7.13 prompt's anti-pattern list: "Don't fix Sprint 6.16.1 audit count (Sprint 7.14)".). **Audit hotfix count unchanged at 16** (Sprint 7.13 is migration milestone, not gate work or hotfix). **NO scope creep confirmed:** `components.css` chrome rules preserved (Sprint 7.14 retirement); `home.css` / `vocabulary.css` / `grammar-wiki.css` redundant chrome CSS preserved (Sprint 7.14 retirement); `user-pill.js` / `theme-toggle.js` `DOMContentLoaded` auto-bind preserved (Sprint 7.14 retirement — their early-exit guards no-op gracefully on migrated pages now that all 18 chrome pages lack inline chrome IDs); body content untouched on all 13 pages (chart MutationObservers, secondary navs, module mount scripts, editorial typography all preserved); no module-loader refactoring (vocab shell mount scripts unchanged); no backend changes; no new component features. **🎯 Architectural milestone:** Before Sprint 7.11: chrome markup duplicated across 18 pages (`~40 lines × 18 = 720 LOC of canonical chrome HTML + ~5 lines per-page binding scripts × 18 = 90 LOC`); chrome contract enforced by cross-page sentinel sweeps (Sprint 6.17 + 6.18 + 6.19 + 6.20 + 7.7-hotfix + 7.8-hotfix cumulative). After Sprint 7.13: 18 pages each carry **2 lines** of chrome reference (`<aver-chrome active="…"></aver-chrome>` + component script tag in `<head>` = `36 LOC across 18 pages`). Net delta from Sprint 7.11+12+13: ~−825 LOC chrome HTML + chrome binding scripts deleted; ~+580 LOC component module (one-time cost). The drift class extinguished at the markup layer: future "chrome inconsistency on page X" cannot recur because there is no duplicated markup to drift. **Pre-push verification:** `grep -rn "aver-chrome" .github/` → no stale CI refs (the existing chrome-unification suite runs in the `frontend` job; no new test file added this sprint).
- Sprint 7.12 (PR #181) — **Chrome migration batch 1/2 — 5 skill landings consume `<aver-chrome>` Web Component.** Phase 3/5 of the chrome unification refactor authorized by Sprint 7.10 approval. Sprint 7.11 (PR #180) built the additive component; Sprint 7.12 migrates the first batch of pages to consume it; Sprint 7.13 migrates the 13 remaining sub-pages; Sprint 7.14 retires `components.css` chrome rules + redundant per-page CSS. Pattern mirrors Sprint 7.3 my-vocab module migration (single-page proof) → Sprint 7.4 / 7.5 batch migrations. **Atomic per-page migrations (5):** **(1) `frontend/pages/home.html`** — chrome markup (lines 45-98, the canonical `<div class="topnav-wrap"><nav class="topnav">…</nav></div>` block) replaced with `<aver-chrome active="home"></aver-chrome>` + new `<script type="module" src="/js/components/aver-chrome.js"></script>` tag in `<head>`. Retired: 4-line inline `<script type="module">` block that did `bindToggleButton(document.getElementById('theme-toggle'))` + `<script type="module" src="/js/user-pill.js"></script>` tag. Kept: Lucide CDN tag + `_renderLucide()` IIFE (body content uses `<i data-lucide>` for hero stat icons), anti-flash IIFE in `<head>` (pre-CSS-load requirement preserved per Phase B Q7). **(2) `frontend/pages/writing-dashboard.html`** — chrome markup (lines 72-111) replaced; Lucide hydration block trimmed (kept `hydrateIcons()` for body Lucide icons; dropped the chrome-icon-rehydration MutationObserver on `data-theme` since chrome SVGs are inline in shadow root now); same script-tag retirements (theme-toggle binding inline + user-pill.js src). **(3) `frontend/pages/speaking.html`** — chrome markup (lines 86-125) replaced with `<aver-chrome active="speaking">`. **Critical refactor — `renderUser()` lines 1018-1041**: page-authoritative pill population (Sprint 7.8-hotfix permissions-context contract) refactored to call `document.querySelector('aver-chrome').setUser({ name: displayName })` instead of directly setting `#user-pill-name.textContent` + computing 1-letter initials on `#user-avatar.textContent`. The component's `setUser()` method handles the canonical 2-letter initials algorithm via `canonicalInitials()` (imported from `/js/user-pill.js`) and writes to the shadow-root elements. Inline `<script type="module">` block that wired theme toggle + chart-refresh MutationObserver split: dropped the `import { bindToggleButton }` + `bindToggleButton(...)` lines; preserved the chart-refresh `MutationObserver` (page-content concern, watches `<html data-theme>` and calls `window._dashboard.refreshCharts()` for Chart.js theme-sensitive axes/grid/tooltip colors), converted from `<script type="module">` to plain `<script>` since the import is gone. Kept `_renderLucide()` for body-content Lucide icons (cards on dashboard). **(4) `frontend/grammar.html`** — chrome markup (lines 73-109) replaced with `<aver-chrome active="grammar">`. Editorial breadcrumb + stats badges secondary nav (Sprint 6.17.2) preserved below the component (page-content concern, distinct from chrome). Retired the canonical theme-toggle + user-pill wiring at lines 263-267. Anti-flash IIFE in `<head>` preserved. **(5) `frontend/pages/vocabulary.html`** — chrome markup (lines 77-123) replaced with `<aver-chrome active="vocabulary">`. Inline `bindToggleButton()` function (defined locally in vocabulary.html line 276, NOT imported from /js/theme-toggle.js — historical inconsistency) deleted. `hydrateIcons()` Lucide function preserved + simplified (kept DOMContentLoaded + load listeners; dropped the explicit `bindToggleButton()` calls). Retired `<script type="module" src="/js/user-pill.js"></script>` tag. **Architectural moment confirmed:** the migration is mechanical — each page replaces ~40 lines of chrome markup + ~5 lines of per-page binding scripts with a single `<aver-chrome active="...">` element + one `<script type="module">` tag. Total LOC deleted across 5 pages: ~225 (estimated 45 chrome markup + 8 chrome script lines × 5 pages); LOC added: ~10 (2 lines per page). Net delta: ~−215 LOC + chrome contract enforcement via the component instead of cross-page drift surveillance. **Tests (extensive sentinel reshaping):** **(a) EXTENDED** `chrome-unification-canonical.test.mjs` — split `CANONICAL_CHROME_PAGES` (was 18) into `MIGRATED_PAGES` (5 with `{ path, active }` records) + `CANONICAL_CHROME_PAGES` (13 remaining sub-pages). Existing per-page chrome contract pins (Sprint 6.17 Phase C1) + Sprint 6.20 Gate 10 `.topnav-wrap` direct-body-child sentinel now iterate over the 13-page legacy roster only. Replaced Sprint 7.11 "no migrations yet" guard block with **two new describe blocks**: "Sprint 7.12 — migrated pages consume `<aver-chrome>`" (5 pages × 5 pins each: aver-chrome tag with correct `active`, component module import, NO inline `.topnav-wrap` markup, NO inline chrome IDs `#theme-toggle` / `#user-pill` / `#user-avatar` / `#user-pill-name` / `#user-menu-logout`, NO inline bindToggleButton import / user-pill.js src, anti-flash IIFE preserved in `<head>` per Phase B Q7); "Sprint 7.12 — legacy chrome pages (13 sub-pages) still inline" guard (13 pages must NOT carry `<aver-chrome>` reference yet — Sprint 7.13 scope guard) + components.css chrome rules still present (Sprint 7.14 retirement) + user-pill.js / theme-toggle.js DOMContentLoaded auto-bind preserved through Sprint 7.12 (Sprint 7.13 refactors). **(b) MODIFIED** `home-redesign.test.mjs` — chrome-markup pins replaced with `<aver-chrome active="home">` + script-tag pin + NO inline `.av-theme-toggle` guard; user-pill IDs flipped from "must exist" to "must NOT exist in page DOM" (moved into shadow root). **(c) MODIFIED** `speaking-redesign.test.mjs` — same per-page chrome flip pattern + **NEW pin** "renderUser delegates pill population to aver-chrome.setUser()" guards the Phase B Q4 contract via regex match on `document.querySelector("aver-chrome").setUser(`. Sprint 6.17.1 "canonical user-pill" pin re-purposed as Sprint 7.12 "moved into shadow root" inversion. **(d) MODIFIED** `writing-dashboard-redesign.test.mjs` — chrome pins flipped; logout pin inverted (`#user-menu-logout` must NOT be in page DOM); Vietnamese microcopy pin updated to remove "Đăng xuất" (now in shadow root, not page-level body microcopy). **(e) MODIFIED** `grammar-wiki-redesign.test.mjs` — `.icon-sun` / `.icon-moon` + `bindToggleButton` pins gate `if (name === 'landing') return;` (4 sub-pages still inline; landing migrated). **(f) MODIFIED** `vocabulary-redesign.test.mjs` — Sprint 6.10 theme toggle + Vocabulary-active + 5-skill-link pins consolidated into a single Sprint 7.12 `<aver-chrome active="vocabulary">` contract pin + inline chrome regression guard. **(g) MODIFIED** `chrome-spacing-canonical.test.mjs` — Sprint 6.18 "18 canonical-chrome-page roster intact" sentinel: 5 migrated pages assert `<aver-chrome active=...>` instead of inline `<nav class="topnav">`, via `MIGRATED_TO_AVER_CHROME` Set guard. **(h) MODIFIED** `theme-toggle-icon-canonical.test.mjs` — `REDESIGNED_PAGES` roster shrunk 29 → 24 (5 migrated pages dropped; component contract covers them in chrome-unification suite). **(i) MODIFIED** `theme-toggle-layout-context.test.mjs` — coverage roster threshold lowered 25 → 24; Grammar Wiki cluster "absolute /js/theme-toggle.js import" pin gates `if (rel === 'frontend/grammar.html')` (landing migrated). **(j) MODIFIED** `css-paths-absolute.test.mjs` — discovery filter extended to include pages matching `'<aver-chrome'` substring so absolute-path sentinel still audits migrated pages' stylesheet hrefs. **(k) MODIFIED** `cross-page-navigation-canonical.test.mjs` `HREF_PINS` — `frontend/grammar.html` row removed (nav-link hrefs now in shadow root; component contract pins all 5 skill hrefs once). **Frontend gate:** 2688/2688 in full frontend suite excluding 5 pre-existing pre-Sprint-7.12 failures (chrome-spacing 6.18 Cat A, ledger 6.16.1 row, subheading 6.19 .eyebrow value × 3) — none caused by Sprint 7.12. 1227/1227 in CI allowlist + extended suites. **Audit hotfix count unchanged at 16** (Sprint 7.12 is migration, not gate work or hotfix). **NO scope creep confirmed:** 13 sub-pages byte-identical (Sprint 7.13 batch 2 scope, guarded by the "legacy chrome pages still inline" describe block); `components.css` chrome rules preserved (Sprint 7.14 retirement); `user-pill.js` / `theme-toggle.js` keep `DOMContentLoaded` auto-bind through Sprint 7.12 (their `if (!toggle) return` / `if (!pillName && !avatar) return` early-exits gracefully no-op on migrated pages where the chrome IDs are not in page DOM); no design system changes; no backend changes; no new component features. **Pre-push verification:** `grep -rn "aver-chrome" .github/` → no stale CI refs (chrome-unification suite already runs in the existing `frontend` job); `package.json` absent (vanilla static HTML confirmed).
- Sprint 7.11 (PR #180) — **Build sprint — `<aver-chrome>` Web Component shipped (additive only; ZERO page migrations).** Phase 2/5 of the chrome unification refactor authorized by Sprint 7.10 approval. Pattern mirrors Sprint 7.3 my-vocab module build (the first DEBT-09-B implementation sprint that delivered ~4h actual on budget). Sprint 7.11 ships the additive component module + source-pin contract + smoke fixture; Sprint 7.12+ migrates pages to consume it. **Phase A audit (JSDOM dependency strategy)** surfaced a clean decision: the repo carries **no `package.json`, no `node_modules`, no transpile step**; CI runs `node --test` directly with the explicit inline-doc principle "*The frontend tests are pure node:test files — no transpile, no bundler. They exercise contracts (HTML structure, CSS token discipline, theme-toggle exports, IIFE patterns) rather than rendered behavior, so a single Node setup with no dependencies is enough.*" Adding JSDOM as a devDep would require creating `package.json` from scratch, an `npm install` CI step, ~5MB of vendored code (alternative), and breaking the "no dependencies" contract for the marginal benefit of render-fixture testing. **Decision: source-pin-only fallback** documented in the Sprint 7.11 prompt as Phase A audit outcome. Trade-off: rendering verification deferred to Sprint 7.12 page-migration smoke (per-page mounted in real browser) + smoke fixture HTML for ad-hoc browser checks. Coverage floor: ~30 source-pin sentinels in `chrome-unification-canonical.test.mjs` cover structural contract + API surface + lifecycle callbacks + token usage. **Atomic deliverables:** (1) **NEW** `frontend/js/components/aver-chrome.js` (~580 LOC, ES module) — `AverChrome` class extends `HTMLElement`, `static get observedAttributes() { return ['active']; }`, attaches shadow root (`mode: 'open'`) on `connectedCallback`, renders the canonical chrome template byte-equivalent to `components.css` lines 741-923 + lines 116-130 inline as `<style>`, idempotent mount via `_mounted` flag, public `setUser({ name?, initials?, email? })` method that writes pill + avatar via canonical 2-letter initials and marks `_userOverride = true` so the auto-fetch path skips. Public `av-chrome-signed-out` `CustomEvent` (`bubbles: true, composed: true`) dispatched right before the `/index.html` redirect. **Imports for reuse:** `bindToggleButton` from `/js/theme-toggle.js` (works on shadow-tree button — `addEventListener` doesn't care about the shadow boundary; teardown function returned for cleanup) + `canonicalInitials` from `/js/user-pill.js` (single source of truth for the 2-letter algorithm; not re-implemented). **Theme-aware icon swap:** `:host-context([data-theme="dark"]) .av-theme-toggle .icon-sun { display: block }` substitutes for the page-level `[data-theme="dark"] .av-theme-toggle .icon-sun` rule — `:host-context()` looks up the tree past the shadow host to find ancestor matches, so the page-level `<html data-theme>` keeps driving the icon swap inside the shadow root (W3C CSS Shadow Parts spec, all major browsers per January 2026 cutoff). **Supabase polling pattern (Phase B Q5):** `POLL_INTERVAL_MS = 50` × `POLL_MAX_TRIES = 60` = ~3s ceiling; recursive `setTimeout(tick, 50)` until `window.getSupabase` is available, then `sb.auth.getSession()` → write pill + avatar if still on placeholder values (`'…'` / `'·'`) and `_userOverride` is false. Silently times out + logs warning if Supabase never appears. **AbortController for cleanup:** dropdown + logout listeners attached with `{ signal }` so `disconnectedCallback()` abort-cancels everything in one call; doc-level click + keydown listeners tracked separately via `_docClickHandler` / `_docKeydownHandler` for explicit removal; poll timer cleared via `clearTimeout(this._pollTimer)`. **Outside-click via `composedPath()`:** the dropdown closes when the click does NOT pass through this host element, using `e.composedPath().includes(this)` — composed events bubble through shadow boundaries so the host comparison is the correct check, not `toggle.parentElement.contains(e.target)` (which is the open DOM idiom that breaks across shadow roots). (2) **NEW** `frontend/tests/fixtures/aver-chrome-smoke.html` — standalone smoke page mountable via `cd frontend && python3 -m http.server 8080`; renders the component with anti-flash IIFE in `<head>` + tokens/base/components CSS + dev controls (active-attribute buttons, setUser buttons, av-chrome-signed-out event log). Smoke does NOT include Supabase init — pill remains on placeholder unless `setUser()` runs. (3) **EXTENDED** `frontend/tests/chrome-unification-canonical.test.mjs` — 2 new `describe` blocks (~30 source-pin tests): **"Sprint 7.11 — `<aver-chrome>` Web Component contract"** pins class declaration + `observedAttributes ['active']` + `customElements.define` registration + 2 module imports (`bindToggleButton` / `canonicalInitials`) + Shadow DOM `mode: 'open'` attach + `setUser` method signature + `_userOverride` flag + `connectedCallback` orchestration sequence (shadow attach → render → applyActive → bindToggle → bindDropdown → bindLogout → schedulePopulate) + `attributeChangedCallback` filters on `'active'` + `disconnectedCallback` cleans up all 5 owned resources + `av-chrome-signed-out` CustomEvent with `composed: true` + `signOut()` in try/catch + `POLL_INTERVAL_MS` / `POLL_MAX_TRIES` constants + `AbortController` usage + `VALID_ACTIVE` enum exactly 5 skills + brand wordmark with `<span class="dot">` + 5 `data-tab` attrs + 2 `.locked` spans (Reading + Listening) + theme toggle SVG icons + user pill + dropdown + 2 menu items + ARIA preservation + `--av-*` token usage in inline style + `:host-context([data-theme="dark"])` for icon swap + `:host { display: block }` + no `window.*` handler leaks + idempotent `_mounted` guard + canonical hrefs on all 5 skill links. **"Sprint 7.11 — additive build only, NO page migrations"** pins zero `<aver-chrome>` references in any of the 18 canonical chrome pages (Sprint 7.12+ scope guard) + `components.css` chrome rules still present (Sprint 7.14 will retire) + `user-pill.js` / `theme-toggle.js` keep their `DOMContentLoaded` auto-bind branches through Sprint 7.11 (Sprint 7.12 refactors them to remove top-level auto-bind so the component owns init). **Tests:** 218/218 in `chrome-unification-canonical.test.mjs` pass (+33 net: 30 new Sprint 7.11 contract pins + 3 new "no-migration" guards). **Frontend gate:** unchanged baseline + 33 new pins. CI allowlist not modified — the extended `chrome-unification-canonical.test.mjs` runs in the same `frontend` job as before (no new test file needed because all pins live in the existing canonical suite). **Audit hotfix count unchanged at 16** (Sprint 7.11 is additive build, not gate work or hotfix). **NO scope creep confirmed:** zero page migrations (all 18 chrome pages byte-identical) — Sprint 7.12 batch 1 (home / speaking / writing-dashboard / vocabulary / grammar) follows; zero `components.css` chrome-rule deletion (Sprint 7.14 scope); zero `user-pill.js` / `theme-toggle.js` refactor (Sprint 7.12 scope); zero slots (Phase B Q3); zero light DOM variant (Phase B Q1); zero footer/sidebar/eyebrow primitives (Phase B Q7). **Architectural moment:** the chrome contract is now provably encapsulated in a single self-contained module. Sprint 7.12 page migrations become **mechanical** — replace the ~40-line chrome block + supporting script tags with `<aver-chrome active="...">` + one `<script type="module" src="/js/components/aver-chrome.js"></script>` — no per-page binding logic, no per-page Lucide hydration MutationObserver, no per-page user-pill populate inline script. The component owns all of it. **Effort:** 3-4h estimated, ~3h actual (faster than estimate because source-pin path avoided JSDOM dependency engineering). **Pre-push verification:** `grep -rn "aver-chrome" .github/` → no stale CI refs (existing chrome-unification suite already runs in the `frontend` job, no new allowlist entry required); `package.json` absent at repo root + frontend root (vanilla static HTML project confirmed).
- Sprint 7.10 (PR #179) — **Approval + prompt sprint — Andy locks Sprint 7.9 Phase B answers and authorizes the Sprint 7.11 build. Pure docs sprint; zero production code modified, zero tests modified.** Follows the discovery→approval→implementation pattern established by Sprint 7.1→7.2→7.3 (which delivered DEBT-2026-05-09-B on the original 22h estimate with 0 hotfixes). Andy's 7 Phase B answers locked: **(Q1) Shadow DOM** — chosen as the platform-level guarantee against CSS override drift (the recurring bug class behind 6.10.1 / 6.15.4-7 hotfixes / 7.8-hotfix vocabulary.css `.brand` override); Shadow DOM closes the class at the boundary because page rules cannot leak into the chrome and chrome rules cannot leak out, while CSS custom properties (`--av-*` tokens) cross the boundary natively per W3C CSS Custom Properties Module Level 1. **(Q2) `active` attribute** — single attribute on the custom element with enum `home` / `writing` / `speaking` / `grammar` / `vocabulary`; "none" is implicit when unset (no `active="none"` ceremony). Reactive via `observedAttributes = ['active']` + `attributeChangedCallback(name, prev, next)` so future page-level state changes can flip the highlighted tab without re-rendering. **(Q3) Slots-free** — no `<slot>` escape hatches on the canonical 18 pages; the contract is "render the canonical chrome exactly, no overrides." Keeps migration mechanical (no per-page slot wiring), and if a legitimate override case surfaces later it can be added in a future minor without breaking the existing API. **(Q4) `setUser(data)` method** — preserves the speaking.html `renderUser` permissions-context override pattern from Sprint 7.8-hotfix through a typed API instead of DOM-poking across the Shadow DOM boundary; placeholder-detection contract (`textContent === '…'` / `'·'`) inverts to "explicit override always wins" inside the component (page-authoritative source defers via method call, not DOM mutation). **(Q5) Polling `window.getSupabase`** — defensive against Supabase init race; matches the existing bootstrap pattern in user-pill.js Sprint 7.8-hotfix (DOMContentLoaded → wait for getSupabase availability → invoke). Avoids hard-failing if the component mounts before the inline Supabase init script runs. **(Q6) Batched migration** — Sprint 7.11 build only (no page migrations) → Sprint 7.12 batch 1 (5 skill landing pages: home / speaking / writing-dashboard / vocabulary / grammar) → Sprint 7.13 batch 2 (13 sub-pages: practice / result / full-test-result / writing-result / my-vocabulary / flashcards / exercises / profile / onboarding / grammar-roadmap / grammar-search / grammar-compare / grammar-article) → Sprint 7.14 cleanup (delete `components.css` chrome block + redundant rules in `home.css` / `vocabulary.css` / `grammar-wiki.css` + retire stale test pins + file Gate 11 candidacy). Total estimated effort ~10-13h across 4 PRs, matching the Sprint 7.1→7.6 phased migration calibration. **(Q7) Explicit out-of-scope:** footer + sidebar (separate non-blocking refactor; chrome ≠ full layout); `.eyebrow` primitive (page-level concern, Sprint 6.19 already shipped); admin pages (out of scope — admin pages have their own chrome family per Phase 4 Cat 5); marketing pages `index.html` / `pricing.html` (out of scope — Cat 4 marketing tier); anti-flash theme IIFE stays in `<head>` per page (must run before any stylesheet to prevent FOUC; the component handles `<style>` inline but cannot own the anti-flash script because the IIFE must execute pre-paint). **Sprint 7.11 build prompt committed** at `docs/sprint-7-11-prompt-aver-chrome-build.md` (~485 lines, 10 sections): scope (in/out), Phase B approvals table, component contract (file layout `frontend/js/components/aver-chrome.js` + `frontend/tests/aver-chrome.test.js`; public API surface `AverChrome` class extends `HTMLElement` + `observedAttributes = ['active']` + `setUser(data)` method + `av-chrome-signed-out` custom event; shadow tree structure mirroring canonical chrome byte-for-byte; inline `<style>` block sourced from `components.css` lines 741-923; lifecycle callbacks `connectedCallback` shadow attach + render + apply active + bind toggle + bind dropdown + bind logout + schedule populate / `attributeChangedCallback` for reactive active highlighting / `disconnectedCallback` AbortController-driven teardown), test strategy (extend `chrome-unification-canonical.test.mjs` with source-pin layer + NEW JSDOM render fixture `aver-chrome.test.js`), phase breakdown (Phase A 30 min audit JSDOM dependency strategy — vendor vs npm-install vs source-pin-only; Phase B 1.5-2h code synthesis; Phase C 1-1.5h tests; Phase D 30 min smoke; total 3-4h), acceptance criteria checklist, NO scope creep list (no page migrations, no `components.css` deletion, no `user-pill.js` refactor, no `theme-toggle.js` refactor, no slots, no light DOM, no footer/sidebar/eyebrow), pre-push verification command (`grep -rn "aver-chrome" .github/ package.json`), PR title template (`feat(chrome): build <aver-chrome> Web Component (Sprint 7.11 — phase 2/5)`), after-merge plan. **Frontend gate:** unchanged from Sprint 7.9 baseline (no test modifications). **Audit hotfix count unchanged at 16** (Sprint 7.10 is approval + docs, not gate work or hotfix). **Open question deferred to Sprint 7.11 Phase A** (not pre-decided): JSDOM dependency strategy — `package.json` carries no `jsdom` dev-dep today, so Phase A audits the three options (vendor JSDOM into `frontend/tests/_vendor/`, npm-install `jsdom` as devDependency, fallback to source-pin-only coverage if neither lands cleanly) before writing the `aver-chrome.test.js` render fixture; source-pin coverage in `chrome-unification-canonical.test.mjs` is the floor, render fixture is the ceiling. **Architectural framing:** Sprint 7.10 is the **approval gate**, not the build gate. Locking the 7 Phase B answers in this ledger row gives Sprint 7.11 a contractual reference — if Sprint 7.11 deviates from any Q1-Q7 decision, this ledger entry is the audit anchor against which the deviation is reviewed.
- Sprint 7.9 (PR #178) — **Discovery sprint — chrome Web Component architecture audit. Pure design work; zero production code modified, zero tests modified.** Pattern mirrors Sprint 7.1 vocab iframe → module discovery (which delivered DEBT-2026-05-09-B on its 22h estimate with 0 hotfixes). Andy approved the direction post Sprint 7.8-hotfix: 18 canonical chrome pages currently copy-paste a ~40-line chrome block + 4-6 supporting script tags; the recurring drift (Sprint 6.17 / 6.17.1 / 6.17.2 / 6.18 / 6.19 / 6.20 / 7.7-hotfix / 7.8-hotfix all touched chrome) keeps re-opening the same class of bug because there's no single source of truth. Sprint 7.9 = audit + design proposal. **Phase A inventory:** 18 pages audited (home / speaking / practice / result / full-test-result / writing-dashboard / writing-result / vocabulary / my-vocabulary / flashcards / exercises / profile / onboarding / grammar / grammar-roadmap / grammar-search / grammar-compare / grammar-article). All ship the canonical `.topnav-wrap` + `.topnav` markup byte-identical post Sprint 6.17 → 6.20 chrome unification — only the `class="active"` placement varies per page. Three CSS files contain chrome rules outside `components.css`: `home.css` (redundant canonical duplicates), `vocabulary.css` (redundant duplicates; 1.35rem `.brand` override already removed Sprint 7.8-hotfix), `grammar-wiki.css` (font-family scoping fix from Sprint 7.8-hotfix — required because the page body switches to DM Sans for editorial typography § 14.2, and the override blocks the body cascade from reaching chrome). **Phase A also documented:** the canonical script-set pattern (Supabase CDN → api.js → initSupabase inline → theme-toggle.js binding → user-pill.js auto-load + Lucide hydration MutationObserver); the user-pill async flow (DOMContentLoaded → bindUserPill + populateUserPill → Supabase getSession → write `#user-pill-name` + `#user-avatar` with placeholder-detection); the eyebrow + secondary-nav scope (page-content level, NEVER inside chrome). **Web Component API designed:** `<aver-chrome>` custom element, **Shadow DOM** (closes the per-page CSS override drift class at the platform level — page rules cannot leak in, chrome rules cannot leak out, CSS custom properties `--av-*` cross the boundary natively), single `active` attribute (enum: `home` / `writing` / `speaking` / `grammar` / `vocabulary`), **slots-free** (no escape hatches for the canonical 18 pages — keeps migration mechanical), **auto-fetch user state** via `window.getSupabase()` matching the existing `populateUserPill` contract from Sprint 7.8-hotfix, **inline `<style>` in shadow root** (FOUC prevention, ~150 LOC self-contained), `setUser(...)` method on the custom element for the speaking.html `renderUser` permissions-context pattern (preserves the Sprint 7.8-hotfix placeholder-detection contract through a typed API instead of DOM-poking across the shadow boundary). **Phased migration outlined:** Sprint 7.10 Andy approval (30 min) → 7.11 build component (3-4h, additive — no migration yet) → 7.12 batch 1 skill landings (5 pages, ~2h) → 7.13 batch 2 sub-pages (13 pages, ~2h) → 7.14 cleanup (delete redundant chrome rules from `home.css` / `vocabulary.css` / `grammar-wiki.css`, delete `components.css` chrome block, retire test pins, file Gate 11 candidacy). **Total estimate ~10-13h** (matches Sprint 7.1 → 7.6 methodology calibration). **Risks documented:** anti-flash IIFE ordering preserved (CSS custom properties cross shadow boundary per spec); Supabase init race (recommend polling pattern); speaking.html renderUser cross-shadow override (recommend `setUser` method); grammar-wiki.css font-family override becomes unnecessary post-migration (Shadow DOM auto-isolates); Vercel rewrite paths require absolute hrefs (Sprint 6.15.8-hotfix lesson); ES module side-effect cleanup (refactor user-pill.js / theme-toggle.js to remove top-level auto-bind so the Web Component owns init); test strategy = source-pin (~80% coverage) + JSDOM render fixture (full coverage). **Recommendation document committed at** `docs/sprint-7-9-chrome-web-component-discovery.md` (~600 lines: problem statement + 18-page inventory + API design + per-page migration diff + risks + effort estimate + Phase B questions for Andy). **Frontend gate:** unchanged from Sprint 7.8-hotfix baseline (no test modifications). **Audit hotfix count unchanged at 16** (Sprint 7.9 is discovery, not gate work). **Predicted long-term impact** (Sprint 7.10 → 7.14 closure): ~720 LOC chrome markup deleted across 18 pages + ~80 LOC redundant CSS removed + ~40 LOC per-page inline scripts retired + ~150 LOC components.css chrome block deletable = ~−850 LOC; ~+250 LOC for the new component module + ~+80 LOC for JSDOM test fixture = **net ~−600 LOC**. Drift class extinguished at the markup layer: future "Sprint X.x-hotfix: chrome inconsistency on page Y" cannot recur because there is no markup duplication to drift.
- Sprint 7.8-hotfix (PR #177) — **RED chrome consistency regression: 3 distinct bugs flagged from Andy's visual review post Sprint 7.7-hotfix merge.** Codex 9/9 GREEN baseline from Sprint 6.17.x → 6.20 was broken by drift that accumulated across the Sprint 7.x module migration cluster. **Bug 1 — logo size drift (3 sizes across 5 pages).** Phase A grep traced two confirmed CSS root causes: (a) `frontend/css/vocabulary.css` lines 66-74 contained an explicit `.brand { font-size: 1.35rem; ... }` override redeclaring the canonical rule with a hardcoded larger font-size and literal font-family (`'Plus Jakarta Sans', ui-sans-serif, system-ui, sans-serif` vs canonical `var(--av-font-sans)` token); pre-existing Sprint 6.10 drift that escaped chrome-unification work and surfaced visually only after Sprint 7.7-hotfix added Tailwind CDN + module CSS to vocabulary.html (the additional imports changed the rendered context enough to make the size difference obvious); (b) `frontend/css/grammar-wiki.css` line 38-51 sets `body.av-page { font-family: 'DM Sans', ui-sans-serif, system-ui }` per § 14.2 editorial sub-system decision — DM Sans was intended for article body content but cascaded into `.brand` because the chrome inherits font-family from `body`. DM Sans renders at visually-different glyph proportions vs Plus Jakarta Sans at the same `1.125rem`, making the chrome look "smaller" on grammar pages. **Bug 2 — user pill stuck on "..."  on writing-dashboard / grammar / vocabulary.** Root cause: `frontend/js/user-pill.js` originally only bound the dropdown toggle + sign-out — it never populated `#user-pill-name` or `#user-avatar`. Population was per-page inline JS shipped on home.html (lines 220-242) and speaking.html (`renderUser` lines 1017-1031), but the 3 broken pages had **no populate logic** so the HTML defaults `…` and `·` stayed forever. **Bug 3 — initials inconsistency.** Same user "Vinh Tran" rendered as "V" on home.html (line 233: `(name[0] || '·').toUpperCase()` — 1-letter) vs "VT" on speaking.html (line 1029: `name.split(' ').map(w => w[0]).join('').slice(0, 2).toUpperCase()` — 2-letter). Two algorithms in production, single source of truth violation. **Phase B canonical decision: 2 letters.** Matches the Sprint 6.17.1 speaking.html implementation, naturally handles single-name case via `slice(0, 2)` (e.g., `"Vinh".split(' ').map(w => w[0])` → `["V"]` → "V"). Supersedes the 1-letter home.html legacy. **Atomic fixes:** (1) **MODIFIED** `frontend/css/vocabulary.css` — deleted the `.brand` rule + `.brand .dot` rule (lines 66-74), replaced with an explanatory comment. Canonical components.css `.brand` (font-size `var(--av-fs-lg)` = 1.125rem, font-family `var(--av-font-sans)` = Plus Jakarta Sans) now wins. (2) **MODIFIED** `frontend/css/grammar-wiki.css` — added explicit `body.av-page .topnav-wrap, body.av-page .topnav, body.av-page .brand, body.av-page .nav-links, body.av-page .topnav-right { font-family: var(--av-font-sans); }` rule after the `body.av-page` DM Sans declaration. Chrome keeps Plus Jakarta Sans even on editorial pages; article body (`.article-body`, headings via Lora) preserves the § 14.2 sub-system. (3) **MODIFIED** `frontend/js/user-pill.js` — extended from dropdown-only binding to dropdown + populate. NEW exports `canonicalInitials(name)` (canonical 2-letter algorithm — single function = single source of truth) and `async populateUserPill()` (reads Supabase session, writes `#user-pill-name` truncated >14 chars + `#user-avatar` via `canonicalInitials`). Idempotent via `data-user-pill-populated` flag; placeholder-detection skips overwrite if a page-specific bootstrap (e.g., speaking.html `renderUser` with permissions context) has already populated the pill — defers to the page-authoritative source. DOMContentLoaded handler runs both `bindUserPill()` + `populateUserPill()`. (4) **MODIFIED** `frontend/pages/home.html` — deleted the duplicated 1-letter inline bootstrap (lines 220-242), keeping only the auth-gate redirect. user-pill.js now owns population canonically. Speaking.html `renderUser` left alone — it correctly produces 2-letter initials matching the new canonical algorithm AND is part of a larger `/auth/me` fetch with permissions context; placeholder-detection in `populateUserPill` defers to it on speaking. **Tests:** (a) **EXTENDED** `chrome-unification-canonical.test.mjs` Sprint 6.17 foundation suite — 5 new pins under `/js/user-pill.js`: exports `populateUserPill` + `canonicalInitials`; canonical 2-letter algorithm signature (`.split(/\s+/)` + `.slice(0, 2)` + `.toUpperCase()`); placeholder-detection (`textContent === '…'` + `'·'`); idempotency guard (`data-user-pill-populated`); auto-bind runs both `bindUserPill()` + `populateUserPill()`. (b) **NEW** describe block "Sprint 7.8-hotfix — `.brand` chrome consistency across pages" — 3 pins: vocabulary.css must NOT redeclare `.brand` (regression guard against the 1.35rem override returning); grammar-wiki.css must keep `.brand` on `var(--av-font-sans)` explicitly (regression guard against DM Sans cascading into chrome again); components.css canonical `.brand` rule uses `var(--av-fs-lg)` + `var(--av-font-sans)` tokens (the source-of-truth pin). **Frontend gate: 186/186 pass in chrome-unification-canonical** + full CI allowlist green. **Audit hotfix count 15 → 16** (Sprint 7.8-hotfix IS audit hotfix — RED visual + functional regression that escaped Sprint 7.7-hotfix's smoke). **Honest retrospection note:** Codex 9/9 GREEN baseline from Sprint 6.17.x → 6.20 was a markup-contract verification — the audit checked that the canonical `.brand`, `.user-pill`, etc. markup existed across pages, but did NOT verify (a) that no per-page CSS overrode the canonical rules (vocabulary.css `.brand` slipped) or (b) that chrome JS contracts had populate parity (user-pill.js missing populate, requiring per-page inline bootstraps that drifted to 1-letter vs 2-letter initials). Sprint 7.x migrations exposed both because Tailwind CDN addition (7.7-hotfix) made the size delta legible and module mounts made the missing populate scripts more visible. Same Gate 11 candidacy filed informally in Sprint 7.7-hotfix applies here: an "embedded-host resource parity + chrome contract verification" gate would have caught both — chrome contract is more than markup, it's the JS+CSS execution that produces a consistent rendered surface. Not formalized this sprint to keep hotfix surgical.
- Sprint 7.7-hotfix (PR #176) — **RED visual regression hotfix: vocab modules unstyled when embedded into vocabulary.html.** Andy reported (post Sprint 7.6 merge) that all 3 vocab tabs (my-vocab / flashcards / exercises) render unstyled when accessed via the unified `/pages/vocabulary.html` landing — buttons in default browser style, all 4 UI states (loading / error / empty / data) stacking simultaneously because `.hidden` Tailwind utility doesn't resolve. **Phase A root cause:** Sprint 7.3 → 7.6 migrations moved each child's page body into JS module template literals BUT did not migrate the resource imports — each standalone shell (`my-vocabulary.html`, `flashcards.html`, `exercises.html`) loads `<script src="https://cdn.tailwindcss.com">` + custom Tailwind config + `/css/<module>.css` + `/css/ds.css` in its own `<head>`; the parent `vocabulary.html` had **none of those imports**, so when modules mount embedded their templates rely on classes that don't resolve. Confirmed via `grep "rel=\"stylesheet\"" vocabulary.html` (only foundation + `vocabulary.css`) vs the 3 standalone shells (each carrying Tailwind + per-module CSS + `ds.css`). Standalone path unaffected — bug is parent-side only. **Why this slipped Sprint 7.3 / 7.4 / 7.5 Phase D smoke:** smoke checks pinned "tab switches smooth" + "module mounts" — they fired the mount + saw the template HTML appear in DOM. Without visual diff vs the standalone shell, the unstyled render read as "it works, looks roughly right." Classic Gate 10 (Sprint 6.20) blind-spot pattern — *markup correct ≠ resource-loaded-correct*. **Phase B option analysis:** **Option A — eager load** (chosen). Add 5 imports to vocabulary.html `<head>`: Tailwind CDN script + canonical Tailwind config (navy/teal + Plus Jakarta Sans, matching standalone shells) + 3 per-module CSS links (`my-vocabulary.css` + `flashcards.css` + `exercises.css`). Simplest fix, smallest blast radius, no module API changes; ~35 KB total additional CSS, cached after any standalone visit. **Option B — lazy inject via `_loader.js`** (rejected): more "module-pattern pure" but requires modifying 3 modules + helper + tests for a hotfix; surface area not warranted for RED visual regression fix. **Option C — inline CSS string in module** (rejected): heaviest refactor, out of hotfix scope. **`ds.css` skipped** — grep confirmed zero `.ds-*` references in any of the 3 modules or their CSS files; the standalone shells link it as historical/cosmetic dead weight. **Atomic fix:** `frontend/pages/vocabulary.html` `<head>` gains 5 imports + explanatory Sprint 7.7-hotfix comment block explaining the contract (modules render embedded ⇒ parent must supply Tailwind + per-module CSS). **Test:** new pin in `vocab-module-loader.test.mjs` section 4 — "Sprint 7.7-hotfix — vocab module resources eagerly loaded for embedded path" — verifies Tailwind CDN `<script>` present, Tailwind config has Plus Jakarta Sans (Sprint 6.10 typography contract), and all 3 module CSS files linked. Drop of any one import fails the test. **Frontend gate: 73/73 in vocab-module-loader pass + full CI allowlist green.** **Audit hotfix count 14 → 15** (Sprint 7.7-hotfix IS audit hotfix — RED visual regression that escaped phased migration's Phase D smoke). **Lesson learned (filed for future Gate 11 candidacy, not formalized this sprint):** when migrating iframe → embedded, the Phase D smoke checklist must include a side-by-side visual diff with the standalone shell — same module, same backend, same theme — to catch resource-loading deltas. Gate 10 (Sprint 6.20) caught chrome position drift across pages; an analogous Gate 11 ("Embedded-host resource parity") would have caught this. Not formalized this sprint to keep hotfix surgical.
- Sprint 7.6 (PR #175) — **DEBT-2026-05-09-B Phase 4/4 CLOSURE — pure cleanup sprint. The vocabulary iframe → module refactor is officially complete.** Final phase of the 4-sprint phased migration. No new features; only deletions + retirements + documentation closure. **Atomic deletions:** (1) **DELETED** `frontend/css/embedded-mode.css` (~50 LOC) — the stylesheet that hid child-page chrome under `html.embedded-mode > body > {header, .topnav-wrap, #vocab-moved-banner}` selectors. (2) **DELETED** `frontend/tests/embedded-mode.test.js` — the Sprint 6.0.1 IIFE sentinel that Sprint 7.5 had already reshaped into pure 3-symmetric-guards (all guards now live in `chrome-unification-canonical.test.mjs` instead). (3) **DELETED** `frontend/tests/iframe-composition-docs.test.mjs` — pinned `DESIGN_SYSTEM.md` § 12 + `TECH_DEBT.md` DEBT-09-B entry content; both have been rewritten as closure narratives, so the pins are obsolete. **Atomic retirements (vocab-landing.js):** (4) Removed `const TAB_SOURCES = { ... }` (was already empty `{}` post-Sprint 7.5). (5) Removed `const _loaded = new Set()` (iframe lazy-load tracker, dead). (6) Removed the entire `else { ... }` branch in `activateTab()` (iframe `frame.src` assignment + `.tab-frame` query). (7) Removed `TAB_SOURCES` key from the `window.__vocabLanding` test seam. (8) Rewrote the header comment to describe the current module mount architecture instead of the historical iframe approach. **Atomic retirements (shell pages):** (9) Removed `<link rel="stylesheet" href="/css/embedded-mode.css">` from `my-vocabulary.html`, `flashcards.html`, `exercises.html` — 3 dead link tags eliminated. Updated each shell's sprint-marker comment to reflect closure. **Documentation closure:** (10) `DESIGN_SYSTEM.md` § 12 (Architectural notes — iframe composition pattern) — rewritten as historical narrative + current module mount documentation (§ 12.1) + retrospective (§ 12.2). The original 3 sub-sections (12.1 composition-not-security / 12.2 un-defer triggers / 12.3 anti-patterns) collapsed into the closure narrative. (11) `DESIGN_SYSTEM.md` § 17.8 Gate 6 (Iframe embedded-mode preservation) — marked CLOSED Sprint 7.6 with pointer to § 12.1 for the current contract; checklist items removed since no page carries the pattern anymore. (12) `DESIGN_SYSTEM.md` § 17.3 sentinel-tests table — dropped `embedded-mode.test.js` entry, added `vocab-module-loader.test.mjs` entry covering the DEBT-09-B closure pins. (13) `DESIGN_SYSTEM.md` audit-checklist line referencing "iframe children Sprint 6.0.1 IIFE first in head" replaced with a closure-pointer comment. (14) `UNIFIED_DESIGN_BRIEF.md` § 1 (scope) exception text updated "iframe panels" → "module panels"; § 2 (phase map) deferral list — DEBT-09-B row moved from active deferrals to closure note; § 3.4 (selector preservation list) `.embedded-mode` bullet replaced with retirement comment; § 3.5 (Iframe contract) rewritten as historical + current module contract pointer; "what's out of scope" bullet updated. (15) `TECH_DEBT.md` DEBT-2026-05-09-B entry — flipped from `DEFERRED` to **`CLOSED 2026-05-13`** with full 6-sprint trail (7.1 discovery → 7.2 approval → 7.3 / 7.4 / 7.5 implementation → 7.6 closure), total effort ~22h matching the Sprint 7.1 estimate, 0 hotfixes required, plus a **reusable methodology block** documenting the discovery/approval/phased/cleanup template for future architectural refactors. Original deferral notes preserved below as historical context. **Tests:** (16) `chrome-unification-canonical.test.mjs` — Sprint 6.17.1 "embedded-mode contract preserved" suite renamed to "Sprint 7.6 — embedded-mode pattern fully retired (DEBT-2026-05-09-B CLOSED)"; deleted the `embedded-mode.css` CSS-selector pin + the `EMBEDDED_PAGES.forEach` "preserves IIFE" loop (both pointed at retired surfaces); kept the 3 symmetric IIFE-retired guards (my-vocab + flashcards + exercises); added new pin asserting no `<link rel="stylesheet" href="/css/embedded-mode.css">` survives on any of the 3 shells. (17) `vocab-module-loader.test.mjs` — 3 "embedded-mode.css link preserved" tests flipped to "embedded-mode.css link removed" (one per shell); the "TAB_SOURCES is empty" test replaced by "TAB_SOURCES + _loaded + iframe branch fully removed" guard pinning each deletion via negative regex; header comment + iframe-related comments swept. (18) `vocab-landing.test.js` — the "TAB_SOURCES is empty post Sprint 7.5" test flipped to "TAB_SOURCES no longer exposed on the test seam" asserting `__vocabLanding.TAB_SOURCES === undefined`. (19) `.github/workflows/backend-tests.yml` — removed both `embedded-mode.test.js` and `iframe-composition-docs.test.mjs` from the CI allowlist. **Frontend gate:** 1101/1101 pass across the CI allowlist + chrome-unification + vocab-module-loader + phase-closure-ledger. **Audit hotfix count unchanged at 14** (Sprint 7.6 is refactor closure, not gate formalization). **Retrospection note:** 4-sprint phased migration delivered on the original Sprint 7.1 estimate (~22h actual vs ~22h estimated), 0 hotfixes required (vs Grammar Wiki cluster's 4 hotfixes for similar architectural complexity). The discovery → approval → phased → cleanup methodology is now a reusable template — documented in DEBT-09-B closure entry for the next architectural refactor.
- Sprint 7.5 (PR #174) — **DEBT-2026-05-09-B Phase 3/4: exercises module migration. MILESTONE — ZERO iframe paths in vocabulary.html. All 3 vocab children on module pattern.** Third of four migration PRs; Sprint 7.6 finishes the debt by retiring `embedded-mode.css` + the legacy iframe branch in `activateTab()` + `TAB_SOURCES`. Phase B Q1/Q2/Q3 approvals carry forward unchanged from Sprint 7.2; no new design decisions. **Phase A audit surprise:** there was NO `frontend/js/exercises.js` — the exercises page is a drill-hub landing (3 cards: D1 fill-blank, Flashcards, D3 speak-with-target) with ~50 lines of inline JS doing auth gate + feature-flag DOM mutation. Cards are plain `<a href>` links to separate drill surfaces (`d1-exercise.html`, `flashcard-study.html`) which are out of scope. No interactive handlers, no timers, no audio, no `window.*` globals, no inline `onclick`. Smallest of the three migrations. **Atomic implementation:** (1) **NEW** `frontend/js/vocab-modules/exercises.js` (~155 LOC) — ES module exports `mount(container, opts) → { unmount }`. HTML body shipped as `const HTML = /* html */ \`…\`` template literal (page-context-bar + 4 render-state divs + 3 drill cards). Render states use `data-state="loading|disabled|error|hub"` attrs (scoped to container, not IDs); cards use `data-card="d1|flashcards|d3"` attrs. **Card hrefs converted to absolute paths** (`/pages/d1-exercise.html`, `/pages/flashcards.html`) per Sprint 6.15.8-hotfix lesson — Vercel rewrites resolve relative paths against the served URL not the rewritten target. `init()` fetches `/auth/me`, sets state via `showState()` helper, applies default-deny DOM mutation (`removeChild` for D1 if flag off, `removeChild` for flashcards if flag off, unhide if on). `unmount()` clears innerHTML + guard — no timers/listeners to clean (no event delegation needed; cards are pure links). Lucide hydrated on mount. (2) **MODIFIED** `frontend/js/vocab-landing.js` — `TAB_LOADERS` gains `'exercises': () => import('/js/vocab-modules/exercises.js')`. `TAB_SOURCES` loses `'exercises'` entry — **now empty `{}`**. Loader-source comment updated to note all 3 children migrated. (3) **MODIFIED** `frontend/pages/vocabulary.html` — exercises tab panel swapped from `<iframe class="tab-frame">` to `<div class="tab-mount" id="mount-exercises">`. **Milestone:** vocabulary.html now ships ZERO `<iframe>` elements. (4) **MODIFIED** `frontend/pages/exercises.html` — thin shell: canonical chrome preserved (Sprint 6.17.1 + 6.18 + 6.19 + 6.20 invariants intact), embedded-mode IIFE retired (Phase B Q3 final closure — `embedded-mode.css` link stays until Sprint 7.6), all inline auth-gate JS removed, body replaced with `<main id="mount" class="vocab-module-mount">`, inline `<script type="module">` imports + calls `mount(document.getElementById('mount'), { embedded: false })`. (5) **DELETED** `frontend/tests/exercises-redesign.test.mjs` (~80 obsolete pins — page body no longer in shell + tests pinned `#state-loading`, `#hub`, `#card-d1` IDs now replaced by data-state/data-card attrs in the module template). **Tests:** (a) **EXTENDED** `frontend/tests/vocab-module-loader.test.mjs` — Section 7 added (~16 pins): exercises module contract (mount/unmount/idempotent guard + 4 render-state attrs + 3 data-card attrs + absolute card hrefs + no onclick + no data-action + no window.* leak + `/auth/me` fetch + default-deny removeChild + unmount cleanup); exercises.html shell pins (chrome preserved + embedded-mode IIFE retired + module-mount script + no inline body markup + no inline /auth/me fetch). Existing TAB_LOADERS pin extended (exercises added); TAB_SOURCES pin asserts **empty** (all 3 children migrated); new milestone pin asserts **zero `<iframe>` elements** anywhere in vocabulary.html. (b) **EXTENDED** `chrome-unification-canonical.test.mjs` Sprint 6.17.1 suite — `EMBEDDED_PAGES` shrunk from 1 → 0 (**now empty**); new pin asserts exercises.html NO LONGER carries embedded-mode IIFE (symmetric to my-vocab Sprint 7.3 + flashcards Sprint 7.4 guards). (c) **RESHAPED** `embedded-mode.test.js` — milestone-driven reshape: no surviving IIFE extraction source remains, so runtime tests retired. File now pure 3-symmetric-guard sentinel: pins all 3 children (my-vocabulary + flashcards + exercises) NOT carrying the IIFE + defensive belt-and-braces pin (no `classList.add('embedded-mode')` anywhere across the 3 children) + preservation pins (`embedded-mode.css` link still present on all 3 shells until Sprint 7.6 + CSS file's chrome-hiding selectors preserved). 5 IIFE-runtime tests deleted + 6 sentinel-only tests retained/expanded. (d) **MODIFIED** `vocabulary-redesign.test.mjs` — "one iframe (exercises only)" → "zero iframes (all 3 vocab children migrated)"; new pin "exercises tab uses module-mount container"; `loading="lazy"` + `referrerpolicy="same-origin"` counts 1 → 0. (e) **MODIFIED** `vocab-landing.test.js` — lazy-mount iframe test deleted (no iframes left to lazy-mount); replaced by 2 new pins: all 3 vocab children registered in `TAB_LOADERS` test seam + TAB_SOURCES empty test seam; topic-bank test description updated (static placeholder, no longer "missing iframe src"). (f) **MODIFIED** `.github/workflows/backend-tests.yml` — `exercises-redesign.test.mjs` removed from CI allowlist. **Frontend gate: 1055/1055 in CI allowlist + vocab-module-loader + chrome-unification pass.** Audit hotfix count unchanged at 14 (Sprint 7.5 is refactor, not audit gate formalization). Sprint 7.6 = pure cleanup (delete `embedded-mode.css` + the 3 stale `<link>` tags + iframe branch in vocab-landing.js + the now-pointless `embedded-mode.test.js` symmetric guards + final lint).
- Sprint 7.4 (PR #173) — **DEBT-2026-05-09-B Phase 2/4: flashcards module migration**. Second of four migration PRs after Sprint 7.3 shipped the foundation. Phase B Q1/Q2/Q3 approvals carry forward from Sprint 7.2; no new design decisions required. **Atomic implementation:** (1) **NEW** `frontend/js/vocab-modules/flashcards.js` (~570 LOC) — ES module exports `mount(container, opts) → { unmount }`. HTML body (formerly lines 117–206 of flashcards.html: fc-header context bar + moved banner + fc-container + create-stack modal + fc-toast) shipped as a `const HTML = /* html */ \`…\`` template literal. State (`_token`, `_state.topics/selectedTopics/selectedCats/previewTimer`, `_toastTimer`) scoped inside `mount()`. All legacy module-scoped vars + handler globals from `/js/flashcards.js` replaced by 7 `data-action` switch cases (open-stack-modal / close-stack-modal / save-stack / open-stack / toggle-topic / toggle-category / delete-stack) dispatched via single container click listener — delete-stack intercepts BEFORE its stack-card parent via `e.stopPropagation()` so the click bubble doesn't open-then-delete. Three input/change listeners delegated on container (`#m-name`, `#m-search`, `#m-after`). Banner visibility driven by `opts.embedded` (no URL sniffing inside the module). `navigateToStudy()` uses `window.top.location.href` when embedded — preserves Phase B Q2 contract for in-tab navigation. Lucide icons hydrated on mount. `unmount()` removes 3 listeners (click+input+change) + clears 2 timers (`_state.previewTimer` + `_toastTimer`) + clears innerHTML + clears guard. (2) **MODIFIED** `frontend/js/vocab-landing.js` — `TAB_LOADERS` gains `'flashcards': () => import('/js/vocab-modules/flashcards.js')`; `TAB_SOURCES` loses `'flashcards'` entry. Exercises remains in `TAB_SOURCES` until Sprint 7.5. (3) **MODIFIED** `frontend/pages/vocabulary.html` — flashcards tab panel swapped from `<iframe class="tab-frame">` to `<div class="tab-mount" id="mount-flashcards">`. Exercises panel unchanged. (4) **MODIFIED** `frontend/pages/flashcards.html` — thin shell: canonical chrome preserved (Sprint 6.17.1 + 6.18 + 6.19 + 6.20 invariants intact), embedded-mode IIFE retired (Phase B Q3 incremental closure — `embedded-mode.css` link stays through 7.5 because exercises still depends on it), legacy `<script src="../js/flashcards.js">` removed, body content replaced with `<main id="mount" class="vocab-module-mount">`, inline `<script type="module">` at end imports + calls `mount(document.getElementById('mount'), { embedded: false })`. (5) **DELETED** `frontend/js/flashcards.js` (408 LOC, superseded by the module). (6) **DELETED** `frontend/tests/flashcards-redesign.test.mjs` (~70 pins — obsolete, page body no longer in shell HTML + read deleted `/js/flashcards.js`). **Tests:** (a) **EXTENDED** `frontend/tests/vocab-module-loader.test.mjs` — Section 6 added (~25 pins): flashcards module contract (mount/unmount/idempotent guard + event-delegation NO window.* leak + 7 data-action values present + 4 category chips + banner-visibility-from-opts + delete-stack stopPropagation + embedded-aware navigateToStudy + unmount cleans 5 paths); flashcards.html shell pins (chrome preserved + embedded-mode IIFE retired + module-mount script + no inline body markup including fc-header/fc-modal/fc-toast/fc-container). Existing TAB_LOADERS pin extended (flashcards added); TAB_SOURCES pin shrunk (only exercises remains); my-vocab tab pin paired with new flashcards tab pin. (b) **EXTENDED** `chrome-unification-canonical.test.mjs` Sprint 6.17.1 suite — `EMBEDDED_PAGES` shrunk from 2 → 1 (flashcards removed); new pin asserts flashcards.html NO LONGER carries embedded-mode IIFE (symmetric to my-vocab Sprint 7.3 guard). (c) **MODIFIED** `embedded-mode.test.js` — `_extractDetectionScript()` source moved flashcards → exercises (last surviving IIFE); cross-page roster shrunk to `[exercises]`; new symmetric guard `flashcards.html no longer carries the embedded-mode IIFE`. (d) **MODIFIED** `vocabulary-redesign.test.mjs` — "two iframes" → "one iframe (exercises only)"; new pin "flashcards tab uses module-mount container"; loading/referrerpolicy counts 2 → 1. (e) **MODIFIED** `vocab-landing.test.js` — lazy-mount iframe test pivoted from flashcards → exercises (only iframe-backed tab exercising the legacy lazy-load path). (f) **MODIFIED** `.github/workflows/backend-tests.yml` — `flashcards-redesign.test.mjs` removed from CI allowlist. **Frontend gate: 838/838 in CI allowlist + 232/232 out-of-allowlist (chrome-unification + vocab-module-loader) = 1070/1070 pins pass.** Audit hotfix count unchanged at 14 (Sprint 7.4 is refactor, not audit gate formalization). Sprint 7.5 (exercises), 7.6 (embedded-mode.css retirement + final cleanup) follow.
- Sprint 7.3 (PR #172) — **DEBT-2026-05-09-B Phase 1/4: module loader foundation + my-vocabulary migration**. First real refactor PR after Sprint 7.1 discovery + Sprint 7.2 approval. Phase B approvals applied: (Q1) event delegation via `data-action="…"` attrs + container click listener — NO `window.*` handler globals leak from the module; (Q2) `window.top.location.href` auth redirect when `opts.embedded === true` (auth loss = top-level event); (Q3) idempotent mount via `data-mounted` attribute guard — second mount() is a no-op, returns the cached handle. **Atomic implementation:** (1) **NEW** `frontend/js/vocab-modules/_loader.js` — shared helper exports `renderSkeleton`, `renderError`, `redirectToLogin`, `guardMount`. Reusable across all 3 vocab modules (7.3 → 7.5). (2) **NEW** `frontend/js/vocab-modules/my-vocab.js` (~700 LOC) — ES module exports `mount(container, opts) → { unmount }`. HTML body (formerly lines 124–280 of my-vocabulary.html) shipped as a `const HTML = /* html */ \`…\`` template literal per Phase B Q2. State (`_token`, `_allItems`, `_currentFilter`, etc.) scoped inside `mount()`. All 11 legacy `window.*` handlers (setFilter / toggleAddForm / submitAddWord / toggleMastery / openReport / closeReport / submitReport / openFlashcardPicker / closeFlashcardPicker / addToFlashcardStack / `window._myVocab`) replaced by 16 `data-action="…"` switch cases dispatched via single container click listener. DOM queries scoped to container (defense-in-depth against multi-mount conflicts). Lucide icons hydrated on mount. `unmount()` removes click listener + clears toast + clears innerHTML + clears guard. (3) **MODIFIED** `frontend/js/vocab-landing.js` — new `TAB_LOADERS = { 'my-vocab': () => import('/js/vocab-modules/my-vocab.js') }` map. `activateTab()` branches: module path renders skeleton via `_loader.renderSkeleton`, dynamic-imports the module, calls `mount(container, { embedded: true })`. Falls through to legacy iframe path for flashcards + exercises (preserved until Sprint 7.4 / 7.5). `TAB_SOURCES` no longer carries `my-vocab` entry. Test seam exposes `TAB_LOADERS` keys. (4) **MODIFIED** `frontend/pages/vocabulary.html` — my-vocab tab panel swapped from `<iframe class="tab-frame">` to `<div class="tab-mount" id="mount-my-vocab">`. Flashcards + exercises panels unchanged. (5) **MODIFIED** `frontend/pages/my-vocabulary.html` — thin shell: canonical chrome preserved (Sprint 6.17.1 + 6.18 + 6.19 + 6.20 invariants intact), embedded-mode IIFE retired (Phase B Q3 incremental closure — `embedded-mode.css` link stays through 7.5 because flashcards + exercises still depend on it), legacy `<script src="../js/my-vocabulary.js">` removed, body content replaced with `<main id="mount" class="vocab-module-mount">`, inline `<script type="module">` at end imports + calls `mount(document.getElementById('mount'), { embedded: false })`. (6) **DELETED** `frontend/js/my-vocabulary.js` (702 LOC, superseded by the module). (7) **DELETED** `frontend/tests/my-vocabulary-redesign.test.mjs` (53 pins, obsolete — page body no longer in HTML) + `frontend/tests/preview-modal-closure.test.mjs` (10 pins, obsolete — preview modal now in module template). **Tests:** (a) **NEW** `frontend/tests/vocab-module-loader.test.mjs` (32 pins) — 5 sections: `_loader.js` exports + helpers; `my-vocab.js` module contract (mount/unmount/idempotent guard + event-delegation NO window.* leak + 16 data-action values present + 7 filter buttons + 2 modals); `vocab-landing.js` TAB_LOADERS branch; `vocabulary.html` mount container + iframe-preserved-for-flashcards-exercises; `my-vocabulary.html` shell (chrome preserved + embedded-mode IIFE retired + module-mount script + no inline body markup). (b) **EXTENDED** `chrome-unification-canonical.test.mjs` Sprint 6.17.1 suite — `EMBEDDED_PAGES` shrunk from 3 → 2 (my-vocab removed), new pin asserts my-vocabulary.html NO LONGER carries embedded-mode IIFE. (c) **MODIFIED** `vocabulary-redesign.test.mjs` — "three iframes" → "two iframes" (post 7.3); new pin "my-vocab tab uses module-mount container"; loading/referrerpolicy counts 3 → 2. (d) **MODIFIED** `chrome-spacing-canonical.test.mjs` Cat A — my-vocabulary dropped from roster (shell `<main id="mount">` carries no padding utilities); new Sprint 7.3 sentinel pins `pt-20 pb-6` inside the module template instead. (e) **MODIFIED** `subheading-pattern-canonical.test.mjs` — my-vocabulary dropped from per-page roster; new Sprint 7.3 sentinel pins canonical "Vocabulary" eyebrow inside `/js/vocab-modules/my-vocab.js` template. **Frontend gate: 2699/2699 pins pass** (net -53 from 2752 because 63 obsolete pins deleted across 2 retired test files, +32 new vocab-module pins, +2 chrome-unification pins, -22 net delta after recalibration). **Audit hotfix count unchanged at 14** (Sprint 7.3 is refactor, not audit gate formalization). Sprint 7.4 (flashcards), 7.5 (exercises), 7.6 (embedded-mode.css retirement + final cleanup) follow.
- Sprint 7.2 (PR #171) — **DEBT-2026-05-09-B architecture approval + Sprint 7.3 prompt creation**. Pure docs sprint: zero production code changed, zero tests changed. Andy reviewed `docs/sprint-7-1-vocab-iframe-discovery.md` and approved all 4 Phase B questions: (Q1) standalone URL preservation is a hard requirement — `speaking.html` lines 937/954/978 + deep links from `d1-exercise.html`, `flashcard-study.js`, `my-vocabulary.js` must keep working; (Q2) HTML template literal inside JS module (single source of truth, no runtime fetch cost, easier testing); (Q3) per-module embedded-mode retirement (each child's IIFE retires when its module migrates; `embedded-mode.css` selector list shrinks progressively, empty at Sprint 7.6); (Q4) phased 4 PRs over Sprint 7.3 → 7.6 (each tab independently shippable; parent keeps iframe path for unconverted tabs during migration). **Sprint 7.3 prompt document drafted** at `docs/sprint-7-3-prompt-my-vocab-module.md` covering: module API contract (`mount(container, opts) → unmount()`), foundation work (loader infrastructure in `vocab-landing.js` + new `_loader.js` helper), first migration (my-vocabulary chosen by Code judgment as highest-traffic / lowest-risk first mover), per-page complexity notes (my-vocabulary.js carries 11 `window.*` handler globals referenced from inline `onclick=""` in card HTML — namespace strategy is the main design surface Phase B must close), 4-phase breakdown (Phase A 1h design + B 30min approval + C 8-9h implementation + D 1-2h smoke), revised effort estimate **~10-12h** for Sprint 7.3 alone (up from the rough 7.1 estimate of ~7h because the window-globals refactor is more involved than initially scoped). **Frontend gate: 2752/2752 unchanged.** Audit hotfix count unchanged at 14 (Sprint 7.2 is docs, not implementation).
- Sprint 7.1 (PR #170) — **DEBT-2026-05-09-B discovery — vocabulary iframe → JS module architecture audit**. Pure discovery sprint: zero production code changed, zero tests changed. Phase A audit (Explore agent, 2h) mapped the current vocab cluster — parent `vocabulary.html` lazy-loads three child iframes (`my-vocabulary.html`, `flashcards.html`, `exercises.html`) with `?embedded=1`; embedded-mode IIFE on each child sets `html.embedded-mode` before CSS loads; `embedded-mode.css` (51 LOC) hides `.topnav-wrap` + banners via direct-child selectors (extended Sprint 6.17.1). The three children are **dual-use**: also linked standalone from `speaking.html` lines 937/954/978 + deep links from `d1-exercise.html`, `flashcard-study.js`, `my-vocabulary.js` — refactor must preserve standalone URLs as hard requirement. Backend API surface = 27 endpoints across `vocabulary_bank.py` (13) + `flashcards.py` (13) + `auth.py` (`/auth/me` for feature flags); **backend orthogonal to refactor, no changes needed**. State is per-page-isolated except `localStorage.av-theme` shared across all pages. Tests pinning iframe contract live in `chrome-unification-canonical.test.mjs:220-251` (embedded-mode IIFE byte-identical on children + `.topnav-wrap` selector in `embedded-mode.css`) and lines 264-306 (Gate 10 anchor sentinel). **4 architecture options analyzed** (Option A full-inline, Option B JS-module dynamic-import, Option C Web Components, Option D iframe-hybrid optimization). **Recommendation: Option B** — each child page module exports `mount(container, opts) → unmount()`; parent uses `await import()` to mount into tab containers; standalone HTML pages keep working as thin shells that mount the same module. Preserves dual usage cleanly, eliminates 3× chrome init overhead, retires `embedded-mode.css` at end of migration, compatible with Aver Design System light-DOM cascade. **Effort estimate: ~22h split across Sprint 7.3 (my-vocab module + parent API), 7.4 (flashcards module), 7.5 (exercises module + feature-flag IIFE migration), 7.6 (embedded-mode cleanup + final test pin updates)**. Phased over 4 PRs because each phase is independently shippable — parent keeps iframe path for unconverted tabs during migration. Sprint 7.2 = Andy approves architecture (30 min). **Frontend gate: 2752/2752 unchanged.** Audit hotfix count unchanged at 14 (Sprint 7.1 is discovery, not implementation). Phase A synthesis document committed at `docs/sprint-7-1-vocab-iframe-discovery.md`.
- Sprint 6.20 (PR #169) — **nav position drift surgical fix + Gate 10 formalization (cumulative 13 audit gates)**. Andy reported chrome shifts vertically by ~24px when navigating between pages — Codex visual consistency audit had reported Sprint 6.19 markup contract 9/9 GREEN, but the position-level drift slipped through because contract tests verify markup, not rendered pixel position. Phase A audit (Explore agent, 45 min) traced the root cause to a 2-of-18 page outlier: `home.html` and `vocabulary.html` nested `<nav class="topnav">` inside `<div class="shell">` (which has `padding-top: var(--av-space-6)` = 24px), while the other 16 canonical chrome pages use `<div class="topnav-wrap"><nav class="topnav">` as a direct body child (no top padding). Both wrappers declare identical `max-width: 1180px; margin: 0 auto`, so horizontal alignment was unaffected; only vertical drift manifested cross-page. Phase B Andy approved **Option A** (surgical HTML restructure of the 2 outlier pages — the canonical pattern is already proven on 16 pages, no CSS changes required, no per-page overrides needed). **Atomic fixes:** (1) **home.html** — `<nav class="topnav">` lifted out of `<div class="shell">` and wrapped in canonical `<div class="topnav-wrap">` as direct body child, before `.shell`; preserves Sprint 6.19 `.eyebrow` markup and Sprint 6.18 spacing tokens; (2) **vocabulary.html** — identical restructure pattern; (3) **DESIGN_SYSTEM.md § 17.14** — NEW Gate 10 (Visual position verification) formalized: origin Sprint 6.20 retrospection, applies to position-sensitive UI / sticky elements / cross-page consistent visual anchors / mixed canonical+bespoke layouts, verification protocol with per-page absolute position measurement at canonical viewport sizes (1440/768/375px), drift threshold >2px, manual smoke checklist template, anti-patterns documenting "markup-correct ≠ position-correct"; (4) **§ 17.12 evolution table** — extended from 5 → 6 blind-spot instances (Sprint 6.20 row added); intro updated "six sprints, six distinct mechanisms"; pattern principle list gained Gate 10 entry; (5) **§ 17.13 consolidation table** — cumulative gate count 12 → 13 with Gate 10 row formalized Sprint 6.20; methodology sections index gained § 17.14 entry. **Tests:** (1) NEW `gate-10-formalization.test.mjs` (12 pins) — § 17.14 section + origin + concept + viewport protocol + pages-list + § 17.12 (6 sprints + intro language + pattern principle) + § 17.13 (cumulative 13 gates + Gate 10 row + all 13 gates referenced) + methodology index; (2) extended `chrome-unification-canonical.test.mjs` with Sprint 6.20 Gate 10 cross-page anchor sentinel (18 pins, one per canonical chrome page) verifying `.topnav-wrap` opens as a direct body child with no `.shell` / `<main>` / `<section>` / `<article>` ancestor open in `head` — HTML comments stripped before depth counting to neutralize Sprint 6.17.2 changelog snippets. **Frontend gate: 2752/2752 pins pass** (+30 net; 2722 → 2752). **Audit hotfix count 13 → 14** (Sprint 6.20 IS counted as audit hotfix because it formalized a new methodology gate, unlike fix sprints 6.17.x / 6.18 / 6.19 which preserved gate count). The chrome contract is now provably cross-page anchored — markup contract + Gate 10 sentinel + future visual smoke combine to prevent this drift class from recurring.
- Sprint 6.19 (PR #168) — **canonical subheading pattern + horizontal alignment + practice modes unification across the 18-page canonical chrome roster**. After Sprint 6.18 closed chrome vertical spacing, three remaining visual inconsistencies surfaced: (1) subheading drift — only 3/18 pages shipped a canonical eyebrow tier label (home, grammar landing, full-test-result, each with a different implementation); (2) horizontal alignment — needed to verify centered exceptions were intentional; (3) practice modes UI — Speaking's 4-tab sub-nav vs Grammar's 3 CTA hero buttons. Phase A audit (Explore agent, 45 min) classified 15/18 pages as MISSING eyebrow, confirmed 3 centered exceptions are intentional (grammar editorial, onboarding signup, full-test-result hero), and identified 4 distinct mode-switching patterns. Phase B Andy approved 4 decisions: (Q1) promote `.eyebrow` from home.css scoped rule to standalone canonical primitive in components.css; migrate `.ftr-eyebrow` page-hero to `.eyebrow` (section-level FTR eyebrows preserve `.ftr-eyebrow` for `--strength`/`--improve` modifier variants); (Q2) per-page eyebrow values (Speaking / Writing / Vocabulary / Grammar Wiki / Hồ sơ / Bắt đầu) + preserve "Overall Band Score" as page-specific domain context on FTR hero; (Q3) preserve grammar.html `.ds-badge.ds-badge-teal` editorial badge (§ 14.2 sub-system intentional — centered + editorial exception protected); (Q4) practice modes KEEP DIFFERENT (Speaking tabs = sub-nav semantic, Grammar buttons = hero CTA semantic — no unification). **Atomic fixes:** (1) **components.css** — canonical `.eyebrow` primitive added (font-size xs, semibold, tracking-widest, primary color, uppercase, margin-bottom space-3); (2) **15 pages gain `.eyebrow` markup:** speaking (×4 tab panels: Dashboard / Luyện tập / Luyện từng Part / Full Test), practice (in `.practice-context-bar`), result (in `.result-header`), full-test-result (in `.ftr-context-bar`), writing-dashboard, writing-result (in `.result-header`), vocabulary, my-vocabulary / flashcards / exercises (in their sticky context bars with `__sep` separator), profile, onboarding (step 1), grammar-roadmap, grammar-search, grammar-compare, grammar-article; (3) **full-test-result.html hero** — `.ftr-eyebrow` "Overall Band Score" class migrated to `.eyebrow`; section eyebrows (`.ftr-eyebrow--strength`, `.ftr-eyebrow--improve`, plain `.ftr-eyebrow`) preserved for distinct muted/success/warning styling; (4) **grammar.html** — NO CHANGES (Phase B Q3 editorial exception). **Tests:** NEW `subheading-pattern-canonical.test.mjs` (28 pins) — foundation `.eyebrow` primitive in components.css with canonical typography tokens, 17 per-page eyebrow text+class pins, FTR hero migration pin pair (canonical + no-regression), grammar.html editorial-badge preservation pin pair, practice modes distinct-pattern pin pair (speaking `.main-tab-nav` 4-tab + grammar `.btn-cta`), alignment exception triplet (grammar text-center + onboarding flex centering + FTR hero text-center). **Frontend gate: 2722/2722 pins pass** (+28 net; 2694 → 2722). Audit hotfix count unchanged at 13 (Sprint 6.19 is fix sprint, not audit hotfix). **Sprint 6.20+ deferred scope:** no follow-up sprint required from 6.19; the canonical chrome contract is now fully unified across the 18-page roster (chrome structure + spacing + subheading + alignment).
- Sprint 6.18 (PR #167) — **canonical chrome vertical spacing unification across the 18-page canonical chrome roster**. Andy reported visible vertical-rhythm drift across redesigned pages: chrome-to-content gaps ranged from 24px (writing-dashboard `py-6`) to 88px (canonical home `.shell` 24px + `.topnav` 64px margin-bottom). Phase A audit (30 min, Explore agent) classified 17/18 pages into 5 drift categories against home.html canonical reference. Phase B Andy approved 4 decisions: (Q1) add 4 new semantic spacing tokens to tokens.css; (Q2) normalize Cat E secondary nav stack to 64px; (Q3) fix Cat F flex-layout pages; (Q4) preserve grammar landing horizontal centering — Sprint 6.18 is vertical-only, Sprint 6.19 (deferred) owns horizontal alignment. **Atomic fixes:** (1) **tokens.css** — 4 canonical chrome spacing tokens declared (`--av-chrome-top-padding: var(--av-space-6)`, `--av-chrome-bottom-margin: var(--av-space-16)`, `--av-chrome-to-content-single: 88px`, `--av-secondary-nav-height: 48px`); (2) **Cat A** (5 pages: profile, writing-dashboard, my-vocabulary, flashcards, exercises) — Tailwind `py-N` wrapper symmetric shorthand replaced with explicit `pt-16/pt-20` + `pb-N` to compensate for absent `.shell` top padding; (3) **Cat B** (1 page: writing-result) — `main.result-content` top padding lifted from `--av-space-6` to `--av-space-20` (80px) to clear sticky `.result-tabs` band; (4) **Cat E** (8 pages: 5 grammar + practice + result + full-test-result) — secondary nav rules (`.gw-subnav`, `.practice-header`, `.result-header`, `.ftr-header`) ship `margin-bottom: var(--av-chrome-bottom-margin)` in their respective CSS files (grammar-wiki.css, practice.css, result.css, full-test-result.css); (5) **Cat F** (2 pages: speaking, onboarding) — speaking.css `.main-tab-nav` ships canonical margin-bottom; onboarding.html primary `<main>` gains `pt-20`. **Tests:** NEW `chrome-spacing-canonical.test.mjs` (42 pins) — foundation tokens declared, components.css `.shell`/`.topnav` unchanged, Cat A wrappers ship pt-N + pb-N (no symmetric py-N regression), Cat B writing-result CSS uses `--av-space-20`, Cat E secondary nav CSS rules use `var(--av-chrome-bottom-margin)`, Cat F speaking + onboarding canonical, 18-page roster coverage with canonical `.topnav` pre-req. **Frontend gate: 2694/2694 pins pass** (+42 net; 2652 → 2694). Audit hotfix count unchanged at 13 (Sprint 6.18 is fix sprint, not audit hotfix). **Sprint 6.19 deferred scope:** horizontal alignment + subheading patterns + grammar landing centered horizontal preservation — explicitly out-of-scope for Sprint 6.18.
- Sprint 6.17.2 (PR #166) — **post-merge visual regression fixes + Grammar cluster canonical migration (Cat 3 exclusion override)**. Three bugs surfaced after Sprint 6.17.1 merge: (Bug 1) `writing-dashboard.html` nav-links wrapped to 2 rows because the canonical `.topnav-wrap` was nested inside `<main class="max-w-3xl">` (768px constraint blocked the 1180px topnav max-width); (Bug 2) `speaking.html` SOON badges rendered with subtle visual drift from the home canonical due to Tailwind CDN preflight `*, ::before, ::after { border-color: currentColor }` reset overriding the `.locked::after` border shorthand; (Bug 3) Grammar Wiki cluster (5 pages) was Cat 3 excluded from Sprint 6.17 chrome unification per Phase A audit recommendation — Andy overrode the exclusion for IA consistency. **Atomic 3-item fix:** (1) `writing-dashboard.html` — lifted `.topnav-wrap` out of `<main class="max-w-3xl">` to be a direct body child (canonical max-width restored); (2) `components.css` `.nav-links .locked::after` — explicit `font-family: var(--av-font-sans)`, `font-weight: var(--av-fw-medium)`, `line-height: 1`, `border-width/style/color` (split from shorthand), and `box-sizing: border-box` to defensively neutralize Tailwind CDN preflight; (3) **5 grammar pages migrated to canonical full nav** (`grammar.html` + `grammar-roadmap.html` + `grammar-search.html` + `grammar-compare.html` + `grammar-article.html`; Grammar tab `.active`) with the editorial breadcrumb + stats badges preserved as a **secondary nav** (`<nav class="gw-subnav">`) directly below the canonical chrome — preserves Grammar Wiki's editorial-tier visual identity (DM Sans + Lora § 14.2 typography untouched, Sprint 6.15.7-hotfix layout-context replaced by canonical `.topnav-right` flex container which the structural sentinel already validates). **Tests:** `chrome-unification-canonical.test.mjs` `CANONICAL_CHROME_PAGES` extended from 13 → 18 (added 5 grammar pages); `cross-page-navigation-canonical.test.mjs` HREF_PINS updated to absolute `/pages/home.html` form on all 3 grammar entries; `theme-toggle-layout-context.test.mjs` Sprint 6.15.7-hotfix wrapper pin replaced by canonical `.topnav-right` flex-container pin (the structural sentinel above already covers all 29+ redesigned pages via `FLEX_HINTS`). **Frontend gate: 2652/2652 pins pass** (+40 net; 2612 → 2652). Audit hotfix count unchanged at 13 (Sprint 6.17.2 is fix sprint, not audit hotfix). **Cumulative chrome unification:** 5 Cat 1+2 reference pages (PR #164) + 10 Cat 2 student pages (PR #165) + 5 grammar pages (this PR) = **20 redesigned pages now on canonical full nav** with consistent skill discovery, user-pill dropdown, and logout flow.

---

## Deleted pages

| Page | Path (former) | Deletion Sprint | Replacement | Vercel Redirect |
|---|---|---|---|---|
| dashboard | `frontend/pages/dashboard.html` | Sprint 5.1 (commit `3f4ff14`) | `frontend/pages/home.html` | 301 → `/pages/speaking.html` (`vercel.json`) |
| landing | `frontend/landing.html` | Sprint 6.13a (PR #145) | `frontend/index.html` | None (was orphan, no production refs) |

---

## Cumulative metrics

- **29 pages redesigned** (4 Phase 1 + 3 Phase 2 + 6 Phase 3 + 16 Phase 4)
- **2 pages deleted** (`dashboard.html` + `landing.html`)
- **Zero pages on `--ds-*` tokens**
- **`ds.css` preserved** as Sprint 6.5.1 compatibility bridge (renderer-emitted `.ds-band-*` / `.ds-crit*` / `.ds-cue-*` on `practice.html` + `result.html`)
- **`admin-writing.css` at-cap** 10/10 `--av-text-faint` (frozen per § 17.6)
- **17 audit hotfixes** cumulative: 6.6.1 (Phase 1), 6.7.1 (writing-dashboard CTA), 6.9.1 (Phase 2), 6.10.1 (theme-toggle icon drift), 6.12c (Phase 3), 6.14c-hotfix (Phase 4 admin), 6.15.2 (narrative correction), 6.15.3-hotfix (Phase 4 closure Gate 8), 6.15.4-hotfix (Grammar Wiki light-theme RED), 6.15.5-hotfix (grammar-article inherited-white RED), 6.15.6-hotfix (Grammar Wiki comprehensive component pass), 6.15.7-hotfix (theme toggle structural drift on grammar sub-pages), 6.15.8-hotfix (CSS path absolutization for Vercel rewrites), 6.20 (Gate 10 formalization + nav position drift), 7.7-hotfix (vocab modules unstyled when embedded — resource imports not migrated alongside iframe → module body migration), 7.8-hotfix (chrome consistency RED: logo size drift via vocabulary.css/grammar-wiki.css overrides + user pill stuck "..." on 3 pages + 1-letter vs 2-letter initials inconsistency — extended user-pill.js to canonical populate with 2-letter algorithm)
- **12 § 17 audit gates** cumulative (Gate 9.5 + 9.6 + 9.7 added Sprint 6.16.1): Gates 1-9 (per § 17.8) + Gate 9.5 (runtime-render inheritance, § 17.9) + Gate 9.6 (structural layout context, § 17.10) + Gate 9.7 (per-component theme verification, § 17.11)
- **5 cumulative audits closed**: Phase 1 (6.6.1), Phase 2 (6.9.1), Phase 3 (6.12c), Phase 4 admin (6.14c-hotfix), Phase 4 closure (6.15.3-hotfix)
- **Phase 4 closure audit:** 0 RED, 2 AMBER closed via 6.15.3-hotfix, 4 GREEN
- **Sprint 6.15.4-hotfix RED bug:** Grammar Wiki cluster light-theme rendering. Root cause: Sprint 6.15 cascade-winning override neutralized only plain `.text-white`, missed 15 Tailwind opacity variants (`text-white/15..90`) + 5 hover + 4 border + 5 bg. Fix: comprehensive opacity-variant override block in grammar-wiki.css mapped to `--av-text-*` / `--av-border-*` / `--av-surface-*` tokens. Same blind-spot class as Sprint 6.10.1 icon rendering miss — § 17 audit-gate methodology note added (visual rendering smoke required in both themes for any cascade-winning override).

---

## Phase 5+ deferred (with un-defer triggers)

| Item | Trigger Reference | Effort Estimate |
|---|---|---|
| Sprint 6.14d-β Tailwind utility-class refactor (admin.html) | DESIGN_SYSTEM.md § 14.5.2 | ~20-30h |
| Sprint 6.14d-γ Per-tab primitive adoption polish (admin.html) | DESIGN_SYSTEM.md § 14.5.3 | ~15-20h |
| admin.html renderer-emitted palette tokenization (4 marked regions) | § 14.5.2 / § 14.5.3 (subset of β/γ) | included in β/γ above |
| ~~DEBT-2026-05-09-B vocabulary iframe → module extraction~~ | ✅ **CLOSED Sprint 7.6 (2026-05-13)** — 4-sprint phased migration 7.3 → 7.6, ~22h actual matching 7.1 estimate, 0 hotfixes | Complete |

---

## Phase 5+ pending decisions (separate planning sessions)

- Commercial launch readiness (Stripe + Email + SEO)
- Pre-launch redirect removal (`pricing.html` `window.location.replace('/')` at head)
- Typography pass sprint (if DM Sans + Lora ↔ Plus Jakarta Sans unification desired — currently sub-system intentional per § 14.2)
- Pricing content alignment with multi-skill landing (Sprint 6.13a-extension)
- Active DEBT closure (DEBT-2026-05-07/08/09 series)
- `ds.css` retirement (Phase 5+ decision — fires only when `.ds-*` class emissions are removed from `practice.html` + `result.html` JS renderers)
- Legacy `frontend/pages/dashboard` 301 redirect removal (low priority — preserves SEO cached links indefinitely; revisit when SEO infrastructure ships)

---

## When to update this ledger

- After every per-page redesign sprint ship
- After every audit closure
- After every page deletion
- After every Phase closure milestone
- After Phase 5+ planning decisions
- Sprint hotfix prompts include a ledger-update step

This ledger is canonical for closure truth. Other docs (`DESIGN_SYSTEM.md` § 14, `UNIFIED_DESIGN_BRIEF.md` § 2, `CLAUDE.md` file-structure table) reference this file. If you find a closure claim somewhere else that contradicts this ledger, the ledger wins and the other doc should be corrected.
