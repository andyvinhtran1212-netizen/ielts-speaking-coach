# Tasks

Tasks are dependency ordered; implementation begins only after this approved spec
is present on the base branch.

- [x] T001 Land pure package validation, builder, immutable source/media checksum checks, source-manifest verification, and negative missing/substituted/extra-input tests. `owns: backend/scripts/build_advanced_vocab_package.py, backend/scripts/sync_advanced_vocab_core30.py, backend/scripts/validate_advanced_vocab_package.py, backend/services/advanced_vocab_package_builder.py, backend/services/advanced_vocab_package_validator.py, backend/tests/test_advanced_vocab_package_validator.py, backend/tests/test_advanced_vocab_package_builder.py` Evidence: 121/121 focused builder/validator/audio/importer tests pass on candidate `44d2941f8`.
- [x] T002 Validate and land the immutable core-30 content/media snapshot plus `source-inputs-manifest.json` and its canonical source revision as a content-only change. `depends: T001; owns: backend/content/advanced_vocab/**, frontend/public/assets/advanced-vocab/**` Evidence: committed v6 T11-map-locked snapshot dry-runs 30/30 lessons valid; deployment reads the immutable snapshot rather than mutable Downloads input.
- [ ] T003 Review migration 281, apply it to staging, and verify it creates only missing Advanced Vocabulary stores while idempotently extending existing course-section evidence; inventory all `quiz_banks` SELECT policies and prove direct anon/authenticated exclusion of `advanced_vocab` with ordinary-bank behavior unchanged, prove the protected-bank `BEFORE DELETE` guard prevents cascade loss, membership-then-assignment-then-item lock ordering, barrier-controlled learner-mutation versus removal, transfer, and archive races in both commit orders, persistence-time membership/publish-at/archive/deadline guards, attempt 1 enforcement, second Reading/Listening rejection even with a different attempt number, same/different replay behavior, concurrent finalization, transaction rollback on an injected finalizer failure, atomic terminal `state='submitted'`, and scoped repair of complete pre-trigger pilot rows including that state. `depends: T002`
- [ ] T004 Merge canonical backend assignment, persistence, learner, and admin result flows only after T003 schema verification, including immutable/idempotent Practice start selection and lost-response reload for both stages, the exact-20 non-empty Controlled Rewrite answer map as one immutable submission with one batch feedback call and persisted provider-failure fallback, the frozen-snapshot `AdvancedVocabularyTarget` in a named/generated shared start-response union, a shared bank-scoped database lock and atomic RPCs for import revision switching, publish-state mutation, and assignment snapshot issuance with final `is_published` revalidation, backward-compatible `/admin/quiz/import` multipart-file and query-parameter transport, `publish_state=preserve|published|unpublished` validation, new-Advanced-bank unpublished default, unchanged new-ordinary-bank published default, existing-bank preservation, and explicit atomic publication directives, protected-bank delete 409/unpublish behavior, transactional archive/republish, active-membership/publish-at/archive/deadline rechecks on every read and mutation, atomic `state='submitted'` plus `submitted_at`/`passed_at` terminal completion with null score, shared learner Review action, runtime-aware neutral admin `completed` summary/tallies with no ledger mismatch, submitted-after-deadline review-only behavior, terminal-expired republish without deadline mutation, admin assignment acceptance, public discovery and generic-play exclusion, ordered-stage rejection, pre-attempt Practice projection, direct/stale-client Writing/Speaking submission rejection with no grading work, preservation of the separate teacher-assignment Writing path, and deletion rejection for every partial-evidence store; define request/response models, regenerate OpenAPI types, and include/pass service/API, assignment-version, RLS, migration, replay/concurrency, bank-revision, bank-unpublish, import/unpublish barrier, AI-call-count/failure/replay, drift, generated-contract, existing import browser-flow, and existing backend regression tests in this same tested SHA. `depends: T003`
- [ ] T004A Resolve the unique persisted `courses.code='C5'` row before import; write its UUID to all 30 Advanced banks; reject a missing/ambiguous course and any `C4`, other-course, or null association; preserve the association in the assignment snapshot; and add importer, database-query, assignment-picker, issuance, immediate-state, and full-reload tests proving exactly 30 Advanced banks are assignable only to `C5` cohorts. `depends: T003; blocks: T005,T006`
- [ ] T004B Build the versioned Controlled Rewrite gold-cohort harness and release report for 60 batch submissions/two per lesson (1,200 answer items split 600 acceptable/600 error-bearing), with dual-human labels and adjudication; freeze first-candidate and solutions-only baselines; record dataset checksum, prompt/model versions, schema/call-count, precision, coverage, false positives, invented quotations, harmful corrections, latency, token usage, and estimated cost; exercise provider failure; and block merge unless every FR-011 absolute threshold passes within the USD 6 evaluation budget. `depends: T004; blocks: T005,T006`
- [ ] T005 Implement learner/admin UI states, dedicated My Class Advanced Start/Continue/Review routing from the generated discriminator, independent Reading panes, immutable Controlled Rewrite answer/feedback/fallback states, and non-submittable Writing/Speaking reference surfaces using generated operation types; include/pass model, component/behavior, Vocabulary completion pending/validation/failure/retry/success/focus/reload, `C5` admin assignment immediate/reload state, archive-only partial-evidence and bank-unpublish actions, keyboard/focus, responsive, interruption/resume, and answer-boundary browser tests in this same tested SHA. `depends: T004,T004A,T004B`
- [ ] T006 Run cross-layer and existing course retry/report regression evidence on the combined candidate, including protected-bank deletion after partial and completed work, direct requests before/at/after `publish_at`, every pre/post-answer-reveal boundary, public listing/generic-play denial, archive/republish before and after expiry for incomplete and terminal submitted items, both import-versus-assignment, unpublish-versus-assignment, default-import-versus-unpublish, and explicit-directive-versus-unpublish commit orders under barriers with post-reload persisted-state/list/picker agreement, initial Advanced-unpublished versus ordinary-published defaults, unchanged dry-run/commit browser flow, invalid-directive no-mutation evidence, the complete FR-011 evaluation/fallback report, exact `C5` bank/snapshot association, and a v1-assignment/v2-bank revision journey across learner, submit, resume, and admin reads. `depends: T001,T003,T004,T004A,T004B,T005`
- [ ] T007 Run independent inline review until clean and record exact-SHA staging evidence. `depends: T006`
- [ ] T008 Obtain explicit owner go/no-go, apply and verify the production migration, run the `Staging promotion gate` immediately before promotion and prove staging HEAD still owns the recorded integrated CI and live Staging E2E evidence, promote staging to main, monitor deploy/health, import the same 30 banks, and smoke production. `depends: T007`
