# Tasks

Tasks are dependency ordered; implementation begins only after this approved spec
is present on the base branch.

- [ ] T001 Land pure package validation, builder, immutable source/media checksum checks, source-manifest verification, and negative missing/substituted/extra-input tests. `owns: backend/services/advanced_vocab_package_builder.py, backend/services/advanced_vocab_package_validator.py, backend/tests/test_advanced_vocab_package_validator.py, backend/tests/test_advanced_vocab_package_builder.py`
- [ ] T002 Validate and land the immutable core-30 content/media snapshot plus `source-inputs-manifest.json` and its canonical source revision as a content-only change. `depends: T001; owns: backend/content/advanced_vocab/**, frontend/public/assets/advanced-vocab/**`
- [ ] T003 Review migration 263, apply it to staging, and verify it creates only missing Advanced Vocabulary stores while idempotently extending existing course-section evidence; inventory all `quiz_banks` SELECT policies and prove direct anon/authenticated exclusion of `advanced_vocab` with ordinary-bank behavior unchanged, prove the protected-bank `BEFORE DELETE` guard prevents cascade loss, membership-then-assignment-then-item lock ordering, barrier-controlled learner-mutation versus removal, transfer, and archive races in both commit orders, persistence-time membership/publish-at/archive/deadline guards, attempt 1 enforcement, second Reading/Listening rejection even with a different attempt number, same/different replay behavior, concurrent finalization, transaction rollback on an injected finalizer failure, atomic terminal `state='submitted'`, and scoped repair of complete pre-trigger pilot rows including that state. `depends: T002`
- [ ] T004 Merge canonical backend assignment, persistence, learner, and admin result flows only after T003 schema verification, including immutable/idempotent Practice start selection and lost-response reload for both stages, the frozen-snapshot `AdvancedVocabularyTarget` in a named/generated shared start-response union, a shared bank-scoped database lock and atomic RPCs for import revision switching, publish-state mutation, and assignment snapshot issuance with final `is_published` revalidation, multipart `publish_state=preserve|published|unpublished` validation, new-bank unpublished default, existing-bank preservation, and explicit atomic publication directives, protected-bank delete 409/unpublish behavior, transactional archive/republish, active-membership/publish-at/deadline rechecks on every read and mutation, atomic `state='submitted'` plus `submitted_at`/`passed_at` terminal completion with null score, shared learner Review action, runtime-aware neutral admin `completed` summary/tallies with no ledger mismatch, submitted-after-deadline review-only behavior, terminal-expired republish without deadline mutation, admin assignment acceptance, public discovery and generic-play exclusion, ordered-stage rejection, pre-attempt Practice projection, direct/stale-client Writing/Speaking submission rejection with no grading work, preservation of the separate teacher-assignment Writing path, and deletion rejection for every partial-evidence store; define request/response models, regenerate OpenAPI types, and include/pass service/API, assignment-version, RLS, migration, replay/concurrency, bank-revision, bank-unpublish, and import/unpublish barriers, drift, and existing backend regression tests in this same tested SHA. `depends: T003`
- [ ] T005 Implement learner/admin UI states, dedicated My Class Advanced Start/Continue/Review routing from the generated discriminator, independent Reading panes, and non-submittable Writing/Speaking reference surfaces using generated operation types; include/pass model, component/behavior, Vocabulary completion pending/validation/failure/retry/success/focus/reload, admin assignment immediate/reload state, archive-only partial-evidence and bank-unpublish actions, keyboard/focus, responsive, interruption/resume, and answer-boundary browser tests in this same tested SHA. `depends: T004`
- [ ] T006 Run cross-layer and existing course retry/report regression evidence on the combined candidate, including protected-bank deletion after partial and completed work, direct requests before/at/after `publish_at`, every pre/post-answer-reveal boundary, public listing/generic-play denial, archive/republish before and after expiry for incomplete and terminal submitted items, both import-versus-assignment, unpublish-versus-assignment, default-import-versus-unpublish, and explicit-directive-versus-unpublish commit orders under barriers with post-reload persisted-state/list/picker agreement, initial unpublished import, invalid-directive no-mutation evidence, and a v1-assignment/v2-bank revision journey across learner, submit, resume, and admin reads. `depends: T001,T003,T004,T005`
- [ ] T007 Run independent inline review until clean and record exact-SHA staging evidence. `depends: T006`
- [ ] T008 Obtain explicit owner go/no-go, apply and verify the production migration, run the `Staging promotion gate` immediately before promotion and prove staging HEAD still owns the recorded integrated CI and live Staging E2E evidence, promote staging to main, monitor deploy/health, import the same 30 banks, and smoke production. `depends: T007`
