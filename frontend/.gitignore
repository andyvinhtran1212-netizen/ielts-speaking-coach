# Sprint 15.2 (F4) — Playwright e2e tooling. Bounded to frontend/tests/e2e/.
node_modules/
playwright-report/
test-results/
.next/
next-env.d.ts
# tsconfig has "incremental": true — tsc drops *.tsbuildinfo next to the
# config; build artifact, never commit (audit F8).
*.tsbuildinfo
